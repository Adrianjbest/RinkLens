// BUILD 714 STATE CONTRACT: authoritative mutations pass through the declared domain owner/reducer; local values are drafts, projections, snapshots or caches only.
//
// 038_RinkLensBuildIdentity.swift
// RinkLens
//
// Central build/version metadata for diagnostics and visible version labels.
// Keep future release-code changes in this one file where possible so small
// patches do not force broad Swift recompilation across route, settings,
// diagnostics, camera and renderer files.
//

// MARK: - RinkLens Build Info

nonisolated enum RinkLensBuildInfo {
    static let semanticVersion = "0.9.8"
    static let releaseCode = "RNG-S12Q-UX16d40b144"
    static let recoveryDNNote = "Recovery DS addresses RL-253 and RL-254: CaptureEngine publishes immutable external-OCR topology requirements, RecordingEngine owns one recording-safe continuation transaction, CaptureLifecycleController alone converges the OCR branch after physical writer closure, and MediaRepository admits the ordered passthrough join only after live capture releases its media lease."
    static let recoveryDTNote = "Recovery DT fixes the b136 physical score/Clock regressions: accepted normal Image Relay scores commit through authoritative MatchState before viewer/event projection, and trusted Clock character bands can re-anchor stale vertical presentation geometry without changing horizontal stability."
    static let recoveryDUNote = "Recovery DV fixes RL-255 and RL-256: penalty-player semantic evidence remains latest-only and freshness-fenced but is no longer starved by each short RecordingWriter frame-critical section; repeated near-zero player crops establish blank evidence and the unsafe no-baseline lifecycle rollout is deleted. HLS/RTMPS publishers physically admit the resolved output cadence before pixel copying and preserve source time. HLS publishing is acknowledged only after aligned audio/video segment evidence."
    static let recoveryDVNote = "Recovery DW/DX fixes RL-257 and RL-258 from the 17:27 physical run: stable low-density scoreboard placeholders can establish the three-sample penalty blank baseline, penalty timer confirmation completes as semantic work before period presentation can displace it, and route visibility no longer rebuilds the persistent Broadcast tree on first entry."
    static let recoveryDYNote = "Recovery DY fixes RL-259 and RL-260 from the 17:48 physical run: timer-first then player penalty execution completes the physical pair before score/period work can age its source frame, and the HLS publisher discards pre-video microphone samples until its first encoded programme video packet establishes the common timeline."
    static let diagnosticArchitecture =
        "typed diagnostics + BroadcastPhase state machine + unified overlay queue + regression harness + SAFE1 governance + COMPOSITE4 WYSIWYG preview + UX1 broadcast fixes + UX2 shared lazy-scroll performance standard + UX3 thin broadcast controls + UX4 scorebug work + UX5 single scorebug template + UX6 shared scorebug/event-popup template sync + UX7 Settings-owned setup tabs, per-team logo backgrounds, profile-safe logo persistence + UX9 canonical scorebug name/layout parity + UX9a compile fix + UX10 standard camera settings, simplified media, manual-event time cleanup + UX11 compact Settings preview, logo-background parity and active-profile save prompts + UX12 clip-press feedback and compact Beside Name scorebug metrics + UX12a Xcode compile fixes + UX12b bottom-video timeline toggle and workflow-ordered Settings tabs + UX12c scorebug game-sponsor right-edge pin + UX12d Settings embedded section chrome and inline-only event popup preview + UX12e Settings compile-time body split and SF Symbol cleanup + UX12r iPad camera source mapping and manual-control restore + UX12s Back Camera virtual zoom source and hidden Ultra Wide selector + UX12t Settings camera source preload + UX12u flat Settings camera cards + UX12v Broadcast/OCR camera sub-tabs + UX12w OCR camera/settings consolidation + UX12x scorebug colour grouping and Settings embedded style polish + UX12y build metadata, top-middle scorebug lock, preview tidy and landscape camera rotation guard + UX12z OCR zone/test flow polish + UX13a simplified Recording screen + UX13b OCR zone alignment and test refresh + UX13c exact OCR crop and calibration letterbox frame + UX13d external camera HD preview format guard + UX13e manual camera resolution/FPS selector + UX13f left-aligned OCR zone selector + UX13g camera settings caption compile fix + UX13h clearer camera format selection and calibration control placement + UX13i external cameras always expose manual Resolution/FPS selection + UX13j safe FPS format guard + UX13k compact OCR diagnostics panel + UX13l compact raised OCR zone selector + UX13m sleep/wake stall guard and camera/OCR recovery + UX13n external black-preview recovery and OCR orientation reassertion + UX13o top-left Test OCR panel and calibration-phase override while zones are visible + UX13p single calibration camera menu, in-camera Advanced OCR access, compact raised zone selector and calibration-phase advanced tuning enforcement + UX13q letterbox-safe bottom zone selector, top Test OCR control and compact diagnostic pill + UX13r real live OCR diagnostics pill above selector, top-only Test OCR crop panel and zone-visible diagnostics publishing + UX13s letterbox-measured calibration chrome: Test OCR images in top black band, diagnostics above bottom selector, selector pinned bottom + UX13t clean OCR camera chrome: landscape-normalised letterbox geometry, top-only Test OCR image pill, and no duplicated per-zone OCR value labels + UX13v removes the OCRSetupView wrapper diagnostics/header chrome so the CalibrationScreen owns a single OCR camera UI: one top Test OCR image panel, one reduced live diagnostics pill, and one bottom selector strip + UX13w makes the Test OCR Raw/Proc/Thresh image panel visible by dismissing the controls sheet before arming Test OCR and reserving fixed top/bottom camera chrome bands + UX13x fixes the OCR camera crash seen on 2026-06-14 by preventing Calibration and camera zoom controls from creating SwiftUI Slider views with invalid/zero-width zoom ranges when an external camera/cable drops or reports fixed zoom + UX13y stabilises OCR top chrome so the Test OCR image panel is top-centred and no longer resizes the camera letterbox + UX13z expands the OCR zone selector and moves OCR running/start-stop to the bottom-right beside the selector + UX14a keeps OCR diagnostics and Start/Stop visible from OCR screen start, moves Centre/Test/Menu into stable top chrome, and defers Test OCR crop processing so the panel opens immediately + UX14b replaces the calibration zoom SwiftUI Slider with crash-safe +/- controls so tiny external-camera zoom ranges cannot assert during OCR camera startup + UX14c increases and reserves the bottom OCR chrome height so the live diagnostics pill is fully visible on first OCR screen load and is no longer clipped by the camera/letterbox boundary. + UX14d pins the Test OCR image panel to the natural top letterbox only, reducing its height so it stays outside the live camera view and no longer drops into the camera image. + UX14e implements the agreed OCR screen wireframe: top status-row owns the centred Raw/Proc/Thresh panel, second top row owns Command Centre/Test OCR/Menu, camera area contains zones only, and bottom chrome owns diagnostics plus selector/start-stop. + UX14f applies the Broadcast-style Command Centre button to OCR, expands the top Test OCR panel to fill the full available top black-bar height, and expands the bottom OCR diagnostics pill to fill the full available bottom black-bar height above the selector/start-stop row. + UX14g removes the selected-zone title/status text from the top Test OCR image panel and lets the Raw/Proc/Thresh preview images use the full available panel height. + UX14t per-rink OCR colour profiles and colour-specific red/yellow/amber/green/blue/dark-light pipelines + UX14u compile fixes for segmented colour-profile calls, async colour-profile capture and stale duplicate debug-panel install guidance. + UX14v compile cleanup removes the remaining MLKit colourProfile call incompatibility and packages all dependency files plus stale duplicate deletion instructions. + UX14x adds a direct OCR Colour Profiles screen with per-zone summary, picker, quick pipelines and per-zone editor. + UX15a makes the bottom OCR values pill use one compact style before/after zones are shown, applies per-zone crop padding to OCR recognition, and publishes selected Test OCR recognition into the bottom diagnostics values. + UX15c adds OCR acceptance and penalty trace diagnostics, improves timer/player recognition ordering, and makes the bottom OCR diagnostics bar show diagnostic raw/cleaned values during Calibration instead of only accepted scoreboard state. + UX15d restores clock, penalty-player and penalty-time recognition by allowing timer OCR to continue past failed segmented attempts, trimming timer crops before seven-segment recognition, and preventing one-digit penalty-player OCR from pre-empting segmented full-number reads. + UX15e adds selected-clock OCR pipeline breadcrumbs, active-pixel metrics and a Calibration override guard so Test OCR cannot inherit Broadcast/intermission clock-only gating while zones are visible. + UX15f publishes selected/test accepted OCR field-state changes so the bottom calibration diagnostics values refresh immediately when Clock, penalty player and penalty time reads are accepted. + UX15g normalises digit-only timer candidates and rejects partial Clock/penalty timer fragments so values such as 2 or :2 cannot overwrite a visible 2:43 crop. + UX15h changes timer recognition architecture to component segmentation first, then seven-segment character decode, then OCR fallback, with component-level breadcrumbs for troubleshooting. + UX15i prioritises the selected Calibration/Test OCR zone before clock bootstrap and always publishes selected-zone failure breadcrumbs when segmentation/OCR does not return a value. + UX15j runs selected Calibration/Test OCR as a direct one-shot crop recognition, keeps diagnostics after route changes, and adds start/result/failure breadcrumbs. + UX15k bypasses the full OCR parser for selected Calibration/Test OCR, runs a true segmentation-only one-shot reader first, and publishes component/active-pixel failure breadcrumbs before any OCR fallback. + UX15l adds lenient component extraction, raw kept/rejected component breadcrumbs and lower-threshold stroke-level seven-segment decoding for thin red/yellow scoreboard digits. + UX15m adds fixed-slot timer decoding for Clock and penalty-time zones so merged seven-segment digit blobs are decoded by expected M:SS/MM:SS slots instead of rejected as oversized components. + UX15n anchors timer slots to the full crop, rejects too-narrow timer bounds, throttles selected Test OCR one-shots, adds segment preview imagery and camera black-screen recovery breadcrumbs. + UX15o anchors Clock and penalty-time slots from the actual colon separator so visible 2:43 style crops split as [minutes] [:] [seconds] instead of generic full-crop ratios. + UX15p forces timer segment preview to one tile per character and rejects timer reads when the expected M:SS/MM:SS slot count cannot be built and decoded. + UX16a introduces a scoreboard-template digit reader for selected Calibration/Test OCR so known digit cells are decoded by expected slot positions before free-form component guesses. + UX16b persists per-rink scoreboard templates with saved digit-slot geometry and enforces MM:SS clock reads when the minutes side contains two digit cells. + UX16b1 forces active build metadata, stores the latest Calibration camera frame for reliable Test OCR, prevents stale accepted values after no-frame reads and adds explicit template/MM:SS entry breadcrumbs. + UX16c1 lets strong active-colour OCR establish a fresh Calibration/Test OCR baseline after stale/implausible previous values, while keeping seven-segment/template fallback gated as diagnostics only. + UX16c2 enforces selected Calibration/Test OCR as OCR-primary only: seven-segment/template output can update segment diagnostics but cannot publish selected Test OCR values, held OCR clears stale selected accepted values, and Calibration OCR state snapshots do not trigger game events or sponsor overlays. + UX16c3 adds an OCR screen stall guard by debouncing zone-selection preview refreshes, throttling Raw/Proc/Thresh/Segments image publishes, and tracing selected Test OCR processing cost. + UX16c3b fixes Swift 6 compile capture errors in the debounced selected-zone preview refresh task. + UX16c4 adds OCR result publication breadcrumbs and a lightweight cleaned-crop digit/timer parser before Vision OCR so selected Test OCR can publish values without re-enabling seven-segment fallback publishing. + UX16c5 makes Test OCR a true single-shot, adds selected-result ownership guards, blocks zone-selection OCR parsing, and adds a dedicated lightweight clock/timer slot parser. + UX16c6 splits the lightweight OCR digit/timer parser out of the large scoreboard view-model to reduce Swift Playgrounds/iPad type-check pressure and first-compile size while preserving UX16c5 behaviour. + UX16c7a rolls back the UX16c7 OCR-entry recovery/watchdog hooks after immediate OCR-screen hang reports, returning OCR entry to the stable UX16c6 path while preserving the parser split and single-shot Test OCR. + UX16c7b adds one target-wide Combine re-export for Swift 6/Xcode explicit-module compilation, resolving cascading ObservableObject and @Published errors without widening runtime code changes. + UX16c7c explicitly imports AVFoundation in the Calibration camera startup selection guard so AVCaptureDevice.Position cases such as .back compile correctly under Xcode Swift 6 explicit-module rules. + UX16c7d adds the required iOS camera and photo-library privacy usage descriptions for Xcode device builds. + UX16c8 stabilises camera-source discovery and selection by separating logical Back/Front/External source IDs from physical AVCaptureDevice IDs, excluding all rear constituent lenses when assigning a second camera, refreshing on device connect/disconnect and reconciling camera lists after asynchronous discovery. + UX16c9 preserves logical camera selections through discovery-only refreshes, prevents transient Picker fallback values from clearing camera roles, replaces an unavailable SF Symbol and hardens diagnostics sharing with a verified temporary copy. + UX16c10 stages camera choices while Settings is inactive, releases competing AVCaptureSession inputs during route hand-off, treats built-in Broadcast plus OCR as a single-session path, removes stale inputs before reconfiguration and records session resource ownership diagnostics. + UX16c11 guarantees OCR Setup camera activation after staged/released selections, waits for competing input release, makes logical Back/Front/External choice authoritative and verifies AVCaptureSession is running before mounting preview. + UX16c12 adds an explicit camera-authorization gate, selects the physical 1x wide camera first for OCR, falls back across equivalent camera devices when input creation fails and exports the exact AVFoundation error domain/code. + UX16c13 commits the OCR route before activation, prevents automatic camera allocation from becoming an operator None selection, allows one built-in logical source across route-exclusive roles and performs bounded visible-route recovery. + UX16c14 moves activation to the mounted OCR view, defers legacy global camera startup on OCR routes and awaits authoritative logical-source staging before permission, input creation and session start. + UX16c15 restores the proven UX16c7a camera startup lifecycle while retaining later logical-source, privacy, fallback, diagnostics, picker and share fixes. + UX16c16 confines all camera ObservableObject publications to the main thread so wake/format refreshes cannot drive SwiftUI or AVCaptureVideoPreviewLayer from camera output queues. + UX16c17 commits Broadcast/OCR routes before camera startup and uses an explicit main-queue bridge for camera frames and OCR results. + UX16c18 validates and rebuilds the AVCaptureSession input/output graph before startRunning and stages the saved logical source synchronously. + UX16c19 restores the known-good retained capture-graph lifecycle, configures camera selections immediately, prevents transient nil Picker values from clearing roles, and uses real preview-layer readiness. + UX16c20 adds forensic camera breadcrumbs for instance identity, route ordering, picker events, permission, discovery, physical-device resolution, capture graph mutations, session notifications, preview-layer/session identity, readiness transitions and first-frame delivery. + UX16c20a fixes Xcode 26.5 compilation by replacing unavailable preview-layer readiness calls with structural readiness diagnostics, declaring the queue-owned camera service nonisolated for delegate compatibility and making OCR region defaults nonisolated. + UX16c21 filters non-video device notifications, retains the External logical Picker row through USB reconnects, invalidates stale external inputs, rebinds a fresh AVCaptureDeviceInput after reconnect and records first-frame luminance. + UX16c22 adds the supplied RinkLens brand image to the Command Centre launch panel as a compact, non-interactive logo card. + UX16c22a embeds the optimised logo directly in Swift so it does not depend on asset-catalog or target resource membership. + UX16c23 replaces competing Broadcast and external-OCR AVCaptureSession ownership with one process-wide AVCaptureMultiCamSession, manually connects both outputs and the Broadcast preview, suppresses legacy recovery while MultiCam owns capture, and preserves OCR clock delivery on Broadcast. + UX16c24 removes the MultiCam preview publication loop, opens Broadcast OCR from active dual-stream frames, makes external-owner UI state idempotent/main-thread only, and guards Photos APIs so missing target usage descriptions cannot crash recording finalisation. + UX16c25 adds an exclusive-input handoff, conservative MultiCam formats, a permanent failure circuit breaker with stable Broadcast-only fallback, honest OCR status, Broadcast recording source integrity and single-line recording controls. + UX16c26 verifies format-level MultiCam support, preflights hardware and sustained pressure cost, keeps video data outputs freshest-frame only, prevents notification-driven reconnect ownership races and compares resolved physical recording sources. + UX16c26a fixes Xcode Float-to-Double compilation at the MultiCam hardware/system-pressure cost error boundary without changing runtime thresholds. + UX16c27 moves AVAssetWriter, render pacing, Core Image composition and frame append to a dedicated serial background worker, refreshes overlay snapshots at low frequency, requires first frames from both MultiCam branches before activation, reports native source cadence honestly and suppresses legacy preview callbacks while MultiCam owns preview. + UX16c28 retains active MultiCam capture and OCR delivery across non-camera routes, transitions to the legacy OCR preview only for OCR Setup, fixes false camera-not-selected state during ownership handoff, restores honest OCR Starting/Running status and confines external-camera quality publication to the main thread. + UX16c29 promotes the existing MultiCam coordinator into a queue-confined capture engine with an explicit lifecycle phase, immutable Sendable snapshots and a separate MainActor UI state store; SwiftUI no longer observes the AVFoundation owner directly. + UX16c30 adds persistent Broadcast and OCR preview endpoints to the same MultiCam graph, mounts OCR Setup directly from the external-camera input port, suppresses legacy preview keepalives while MultiCam owns capture and removes route-driven MultiCam-to-legacy OCR handoff. + UX16c31 moves every camera-session start, stop, release, wake reassert and preview-recovery decision behind one CaptureLifecycleController; the ViewModel and SwiftUI now submit typed capture intent only. + UX16c32 introduces a bounded capacity-one FrameHub for Broadcast and OCR frames, migrates recording and Test OCR latest-frame reads to the hub, retains service caches as compatibility fallbacks and exports per-role frame freshness diagnostics. + UX16c33 extracts OCR queue and processor ownership into a dedicated orchestration engine, routes continuous and selected-zone OCR through typed asynchronous requests, preserves the existing recogniser chain and exports orchestration diagnostics. + UX16c34 adds a pure unified MatchState reducer, moves accepted scoreboard writes behind one ViewModel commit boundary, centralises manual-vs-OCR protection and routes score, period, penalty, reset and local-clock transitions through typed actions with reducer diagnostics. + UX16c35 deletes legacy Broadcast/OCR capture execution and preview hosts, makes the single CaptureEngine own dual-camera, Broadcast-only and OCR-only graphs, and routes recovery, reconnect and degradation through that owner. + UX16c35a restores frame-delivery policy state and applies the Stage 7 Xcode compile/concurrency cleanup. + UX16c35b fixes the remaining MainActor call-site compile failure, isolates capture-engine value contracts from the app default actor, boxes CoreMedia/CoreVideo captures for Sendable queue handoff and removes the lens-audit unlock warning. + UX16c35c makes the Debug Logging diagnostics mode selector first-tap responsive with proper SwiftUI observation, optimistic MainActor publication and stale-snapshot protection. + UX16c36 makes CaptureEngine mode authoritative, selects dual-camera on Broadcast whenever OCR is requested, coalesces duplicate lifecycle commands, applies camera and exact-format changes as stop-reconfigure-resume transactions, lists every MultiCam-compatible resolution and integer frame rate, rejects stale OCR frames by generation/device, guarantees OCR cancellation cleanup, derives ownership/status diagnostics from CaptureEngine and FrameHub, and fixes 30fps render-pacer interval reporting. + UX16c37 adds token-owned OCR passes, full typed camera/format contract validation after in-flight transitions, typed active CaptureEngine formats and complete camera/format selection rollback with previous-graph restoration. + UX16c37a fixes the Calibration startup camera fallback Xcode type error by returning the first CameraOption identifier rather than the option value. + UX16c37b fixes the external OCR disconnect Broadcast-only recovery compile error by preserving and passing the typed Broadcast format preference while explicitly clearing the OCR format preference. + UX16c37c relocates the concrete MultiCam coordinator into the target-owned capture-engine state source so the coordinator type cannot be lost through separate file target-membership or source-set visibility. + UX16c38 makes capture intent revisioned and latest-request-wins, abandons stale queued/in-flight lifecycle commands, replaces polling first-frame waits with frame-callback readiness leases, uses monotonic DispatchTime deadlines and removes Thread.sleep from the authoritative CaptureEngine session queue. + UX16c39 persists failed dual-camera contracts through Broadcast-only fallback, applies a failed-contract cooldown with an explicit operator Retry action, records AVFoundation dropped-frame reasons per branch, exports drop counters and observes live camera system-pressure state. + UX16c39a fixes iOS 18.5 compilation by treating SystemPressureState.Level as a RawRepresentable value rather than an exhaustively switchable enum. + UX16c40 adds conservative pressure-driven OCR throttling/suspension while preserving Broadcast, exact CMTime cadence contracts including 29.97/59.94, and Common/Advanced camera format views. + UX16c41 classifies camera changes by disruption cost, applies same-device/same-resolution cadence changes live, keeps zoom/focus/exposure/white-balance as live controls, and reserves full graph rebuilds for mode, device, resolution or failed-live-mutation recovery. + UX16c41a removes the Advanced resolution list and presents only camera-supported 720p, 1080p and 1440p choices at operator-facing 30 or 60 fps while preserving exact rational cadence internally. + UX16c41b makes team/profile text fields first-tap stable, resets transient logo picker selections after every load so the same image can be selected again, and removes Broadcast/OCR zoom controls from Settings Camera because zoom is owned by the live camera screens. + UX16c41c makes manual format discovery role-independent and automatic, fixes first-tap camera toggles, coalesces repeated camera recovery, exports logs as a single asynchronous stable file, defers heavy Command Centre diagnostics behind a branded startup splash and audits Settings toggles for synchronous publication. + UX16c42 separates logical, preferred and active camera identity, centralises one DesiredCaptureContract, suppresses identical reconciliation, accepts Apple-resolved camera constituents, stores the effective graph contract and retains capture through temporary scene inactivity. + UX16c42a fixes the Xcode 26.5 compile failures and concurrency diagnostics introduced by the capture-contract authority patch. + UX16c43 removes retained CMSampleBuffer forwarding, stores one latest CVPixelBuffer per camera role, gives Test OCR a monotonic fresh-frame waiter and exclusive pass token, publishes explicit outcomes and adds operator-confirmed Apply Test Result. + UX16c43a fixes the remaining Diagnostics FrameHub call-site mismatch after the monotonic uptime API migration. + UX16c44 separates continuous Broadcast, Calibration selected-zone and Test OCR publication flows; only continuous OCR commits through MatchState, while per-field diagnostics show recogniser evidence, reducer outcome and visible scorebug value. + UX16c45 adds a recording-owned Broadcast capture lease, current-generation source preflight, bounded source-loss and low-cadence fail-fast finalisation, separate camera/writer/render drop counters, partial-file preservation and explicit recording health diagnostics. + UX16c46 hardens route-scoped lifecycle ownership, sustained-divergence health reconciliation, exact OCR runtime statuses, coherent active camera identity, generation-scoped drop counters, capped breadcrumbs and coalesced UI publication. + UX16c47 adds a non-invasive physical validation harness with timed OCR/Broadcast/dual-camera soaks, USB and scene correlation, 1 Hz cadence/memory/drop/lifecycle/OCR/recording evidence, stale Test OCR fencing counters, operator checkpoints and report/CSV export. + UX16c48 adds a controlled training-session pilot with camera-role preflight, Engineering-to-Rink-Test logging transition, continuous OCR/scorebug and full-match recording evidence, manual clip confirmation, controlled scene testing, recovery-action tracking and in-capture All Logs export verification. + UX16c49 adds a UX16c48-gated Match Day Safe fixture pilot with saved-profile preflight, Broadcast-only fallback confirmation, continuous capture/OCR/recording/request/buffer/score-event monitoring, period-break and post-match All Logs evidence, and persisted two-or-three-clean-fixture promotion gating. + UX16c50 hardens physical validation with stable-graph preflight, FrameHub cadence evidence, stable-route request-storm accounting, OCR token-age stall recovery and typed source-loss versus severe-cadence recording failures. + UX16c50b fixes the Xcode 26.5 validation CSV type-check timeout, removes invalid property-level preconcurrency annotations and adopts the iOS 26 video-composition configuration API. + UX16c50c removes the failed nonisolated property-wrapper approach, restores coalesced manual ObservableObject publication, fixes the clip async bridge scope, and clears the remaining Swift 6 sendability and unused-state diagnostics from the 19:13 build. + UX16c50d fixes the manual post-roll completion closure receiver exposed by the 19:22 Xcode build. + UX16c50e restores required controlled-pilot dependencies at their correct local scopes after the 19:27 Xcode build exposed over-aggressive unused-variable cleanup. + UX16c51 rotates blocked OCR workers instead of queueing behind retired Vision work, distinguishes recording source availability from sampling duplicates, oversamples the source mailbox without inventing frames, reconciles configured/effective/observed camera cadence and treats rink thermal pressure as contextual unless measurable performance is affected. + UX16c52 prevents black-frame telemetry from impersonating source loss, starts recording only after the first encoded frame, isolates manual-clip buffers behind a capacity-one dedicated pool, and reports same-camera OCR fallback truthfully. + UX16c52a removes the permanent 30fps manual-clip cap, targets the active recording cadence and preserves real-time clip duration when the capacity-one mailbox replaces frames under backpressure. + UX16c52b removes the stale quality-contract compile dependency and confines AVAssetWriter status reads to the recording worker queue for Swift 6 sendability. + UX16c53 aggregates AVFoundation drop telemetry off-main, replays the latest capture contract after the recording lease releases, preserves continuous OCR across non-camera routes, records typed recording stop origins, and exports generation-scoped physical-camera/cadence/luminance truth. + UX16c53a completes the OCR Deferred by Recording status integration across Command Centre health, operational health and Calibration status colour switches for Xcode exhaustive-switch compilation. + UX16d1 establishes behaviour-neutral composition injection, single runtime/diagnostics snapshots, a passive telemetry contract, architecture gates and XCTest foundations. + UX16d1a resolves the AppCoordinator MainActor default-argument construction error exposed by the Xcode iPad build. + UX16d2 makes route changes presentation-only, merges recording mutation policy into the capture lease and retires obsolete camera ownership guards. + UX16d2a gives OCR an immediate owned-frame copy and one non-rotating executor, reasserts and verifies the authoritative OCR cadence after MultiCam start without downgrading Broadcast, and makes the first failed writer append terminal with full error evidence. + UX16d2b removes eager second-orientation and synchronous framework fallbacks from continuous OCR, records the exact processor stage and reports stalled OCR truthfully. + UX16d2c separates continuous OCR observations from committed MatchState, confirms field-specific live transitions before event side effects, fences OCR-driven intermission/sponsor/strength changes and recomputes the current capture contract when the recording lease ends. + UX16d2d makes callback freshness part of capture health, reconnects a silent OCR output once before a bounded graph rebuild, preserves Broadcast recording during OCR recovery and removes identical no-op health reconciliation storms. + UX16d2e keeps score/period/penalty zones under cheap live hash watch, opens bounded deterministic recognition only for changed fields, supports running-clock confirmation of plausible +1 goals and paired penalties, and caps timer/digit segmentation variants. + UX16d2f unifies Test and continuous OCR behind one border-cleaned bounded decoder, establishes a side-effect-free live baseline, defers calibration image rendering during recording and makes cadence fail-fast depend on fresh-frame plus output truth. + UX16d2h pins clip segment references during recording, defers all manual/automatic clip export until the full-match writer finalises, prefers passthrough remux for manual clips and removes misleading background export labels from main-thread stall context. + UX16d2g replaces active-pixel OCR geometry with persisted per-rink character cells, requires strict two-style agreement without confidence inflation, enforces structural MM:SS/M:SS layouts and establishes side-effect-free baselines independently per field. + UX16d2g1 performs one perspective-corrected raw-board render per pass, derives independent colour and contrast masks on the OCR worker, rotates bounded field work, drains cancelled continuous work before Test OCR, and lowers OCR budgets while recording. + UX16d2g2 restores the intended 1920x1080 landscape contract for automatic Broadcast/OCR capture, converts preview and field geometry into the rectified-board coordinate space, makes Calibration previews use the decoder crop, and gives Test OCR exclusive executor admission. + UX16d3 consolidates full-match recording, writer pacing, PixelBuffer clip capture and media persistence behind AppContainer-owned RecordingEngine, RecordingWriter, ClipEngine and MediaRepository authorities, with typed stop reasons and deletion of the obsolete live clip exporter. + UX16d3a removes the launch-time singleton recursion by installing the AppContainer-owned RecordingEngine and ClipEngine compatibility bridges before HockeyScoreboardViewModel bootstrap. + UX16d4 Build 502 replaces the failed OCR patch chain from the Build 498 core and includes actor-safe compile corrections: synchronous token cancellation, FrameHub-only Test OCR, per-cell two-mask decoding, immediate Test-and-Apply publication, and no route-triggered Test capture rebuild. + UX16d4 Build 503 optimises the same core per-cell decoder so live 0.42-0.65s budgets are not consumed by repeated slot/reference searches and distinguishes deadline expiry from genuine colour/contrast disagreement. + UX16d5 Build 504 keeps saved field zones but replaces production fixed-cell/colon-slot recognition with bounded dynamic token discovery, independent 0-9/colon classification, post-recognition field validation and frame/generation token breadcrumbs; seven-segment remains diagnostics-only. + UX16d6 Build 505 cleans field-border artefacts before token discovery, recovers one-dot/two-dot timer separators, recognises each digit with rink-neutral per-token segment topology, preserves glyph aspect ratio for generic fallback and removes rink-specific bitmap examples from the production decision path. + UX16d7 Build 506 locks OCR geometry to the Calibration video-aligned 16:9 contract, replaces isolated 8x8 point sampling with perspective-corrected whole-zone block-average perceptual hashing, rectifies once for all watched fields, and short-circuits strong colour-mask reads so clock recognition cannot starve paired penalty fields. + UX16d7a Build 507 restores the processor-local normalized-rectangle rotation helper required by the rectified-zone path under Xcode target compilation. + UX16d8 Build 508 adds evidence-locked game-clock direction, moving-clock anchor resynchronisation, dedicated per-score confirmation windows, dynamic-token publication evidence and font-shape/hole agreement for solid display digits. + UX16d9 Build 509 makes the physical scoreboard the only game-clock authority, removes synthetic game-clock mutation, publishes only complete colour/contrast-agreed OCR reads, holds one-frame reversals and large jumps for confirmation, and makes score confirmation single-stage and time-bounded. + UX16d10 Build 510 distinguishes confirmed dual-mask evidence from provisional strong-colour evidence, bounds pending clock evidence to three seconds/three observations, compares clock formats canonically and suppresses repeated unchanged-clock side effects. + UX16d11 Build 511 adds bounded single-unknown clock-token recovery, expanded neutral score reacquisition after empty crops, and two-read event-free physical score baseline synchronisation. + UX16d12 Build 512 gives continuous clock OCR Test-mode parity, alternates a dedicated full-budget clock pass with a reserved unresolved score/period baseline burst, preserves an 80ms timer recovery reserve, retains narrow centred score digits such as 1, and marks stale Calibration confidence as unverified. + UX16d13 Build 513 makes continuous Broadcast OCR alternate deterministically between Clock and full Home/Away/Period verification even after an early baseline is marked established, gives Test and continuous OCR the same bounded decoder/recovery policy, repairs repeated high-trust period display alignment without intermission events, and exports pass, field, confirmation, Test-versus-live and reducer breadcrumbs. + UX16d14 Build 514 seeds Clock authority from operator-confirmed Test OCR, admits ordinary single-source sequential continuation, resolves invalid time only from classifier alternatives consistent with the trusted anchor, uses Clock/Clock/static acquisition cadence until stable and persists a twelve-pass evidence ring. + UX16d15 Build 515 recognises the scoreboard solid-font 1 before closed-loop topology can call it 9, allows only sequential +1 live score increases, holds implausible Test OCR score/period jumps for explicit confirmation, preserves operator-confirmed static baselines across capture-generation changes and makes pending baseline acquisition genuinely fieldwise. + UX16d15a Build 516 adds an append-only Engineering OCR evidence journal containing every processed accepted/rejected dynamic-token attempt and every publication/reducer decision, with rotating JSONL persistence and complete All Logs export. + UX16d15b Build 517 adds committed zone-edit geometry auditing, per-field zone revisions and complete score-field reducer transition history so physical-side zone swaps can be correlated with every OCR token, publication and visible score outcome. + UX16d15c Build 518 preserves static score and period confirmation across the real scheduler cadence, commits verified sequential goals to Broadcast, retains pending evidence across low-trust gaps and separates recognised OCR values from committed live values in Calibration diagnostics. + UX16d15d Build 519 makes Test & Apply atomic for every parser-accepted field, permits deliberate operator score/period resynchronisation, seeds the confirmed field baseline immediately, and lets accepted structural single-mask score/period reads start provisional initial-baseline evidence that a matching later read can complete. + UX16d15e Build 520 gives pending score and penalty observations an immediate verification pass, resolves incomplete clock tokens from ranked morphology votes against the physical-board clock anchor, accepts complete monotonic single-source clock continuation, reduces paired penalty confirmation to two reads, removes player-zone edge artefacts, enforces M:SS penalty structure and adds end-to-end event/overlay auditing. + UX16d15f Build 521 classifies every timer token before rightmost-first bounded recovery, gives confirmed goals immediate popup eligibility independent of clock authority, releases legacy held events after a bounded timeout, and shifts only penalty-time crops inward when rectification lands exactly on a board edge. + UX16d15g Build 522 separates timer recognition into fixed-cost topology, targeted generic and bounded morphology stages, reserves fair timer-mask budget, short-circuits structurally complete single-mask timers into bounded confirmation, and keeps a provisional Clock on immediate Clock-only passes until accepted or expired. + UX16d15h Build 523 pushes accepted Broadcast overlay snapshots directly into the live runtime, traces the Clock reaching the runtime and preview compositor, lazily creates contrast masks only when useful hard-deadline budget remains, gives colour timer decoding the authoritative field deadline, and prevents continuity-repaired or weak Clock evidence from confirming a stopped board. + UX16d15i Build 524 makes every continuous Broadcast OCR pass Clock-first, limits static recognition to hash-triggered work plus a rolling five-second audit, fuses colour and contrast evidence independently by timer slot, resolves false hole-only 8 classifications through middle-segment evidence, and adds a Broadcast-only two-second presentation holdover that never mutates MatchState. + UX16d15j Build 525 replaces the shared static audit cursor with independent Home, Away and Period deadlines, gives each scheduled static field a full-budget pass instead of placing it behind the Clock decoder, prioritises score verification, invalidates edited-zone evidence fieldwise, and separates diagnostics-only Verify Zone from an explicit Apply Manual correction action. + UX16d15k Build 526 holds OCR goal and penalty popups until the physical clock has run continuously for five seconds, cancels pending popups when accepted board values are corrected during that window, removes timeout-based premature display, and gives changed penalty player/time pairs a dedicated full-budget OCR pass ahead of routine score audits. + UX16d15l Build 527 consolidates game-clock direction, reset, stop and smoothing decisions into the trusted physical-board authority, prevents delayed OCR anchors from rewinding Broadcast presentation, preserves running state when OCR is merely late, requires three-read stopped confirmation, enforces explicit direction, and requires fresh running evidence before delayed goal or penalty popups are released. + UX16d15l1 Build 528 fixes the GameEventDetector optional-binding shadow that prevented Xcode from clearing post-restart popup evidence after release. + UX16d15m Build 529 adds bounded atomic full-board reset recovery, pending-until-verified visual hashes, guaranteed Period scheduling, timer-position 0/8 repair, malformed-penalty urgency expiry, stale Clock truth and per-event five-second popup eligibility. + UX16d15m1 Build 530 restores the stopped-clock perceptual-hash helper removed during the pending-hash refactor, resolving the Xcode missing-symbol compile failure without changing Build 529 runtime behaviour. + UX16d15n Build 531 uses scoreboard-controller ground truth to confirm running penalties from stable-player timer progression, recover closed-loop 8/0 Clock candidates before they poison the trusted anchor, require distinct reverse movement, filter isolated score/player edge tokens and cool malformed empty penalty pairs. + UX16d15o Build 532 integrates persistent manpower into the scorebug, confirms active penalty clears from fresh blank pairs, correlates delayed power-play goals with recent clears, and prioritises initial score/period baselines. + UX16d15p Build 533 preserves pending score verification until resolved, restores fair penalty-pair scheduling, synchronises existing penalties event-free when Broadcast OCR arms, rejects conflicting score masks, and safely catches up a missed two-goal score gap. + UX16d15q Build 534 restores continuous all-field Calibration rotation, reserves selected-zone recognition for explicit Test OCR, preserves score-versus-penalty fairness across Clock bridge passes, guarantees overdue score audits cannot be starved by persistent penalty noise, and rejects short single-token player artefacts that touch the extreme crop edge. + UX16d16 Build 535 replaces the split Broadcast OCR phase/fairness scheduler with one typed production control plane, schedules Clock, Home, Away, Period and complete penalty pairs by hard service deadlines, records attempt/completion/usable timestamps separately, keeps failed work overdue with bounded backoff instead of treating admission as success, and removes selected-zone scheduler state from continuous OCR. + UX16d16a Build 536 makes Clock confirmation transactional and pre-emptive, restores bounded Clock/static alternation, lets a descending penalty timer prove its own running sequence before main-Clock authority is established, and simplifies Diagnostics to four status tiles plus Camera/OCR/Recording/Logs with advanced engineering tools collapsed. + UX16d16b Build 537 keeps trusted Clock movement as the preferred popup release, adds a ten-second repeatedly-confirmed score-stability fallback for OCR goals when Clock authority is unavailable, cancels fallback events when the scoring side is corrected below the confirmed post-goal score, and prevents a later goal by the other team from invalidating an earlier pending goal popup. + UX16d16c Build 538 separates unresolved penalty baseline acquisition from real publication confirmation, guarantees bounded Home/Away/Period service, rotates equally-due failed work fairly, immediately rechecks actual pending score/penalty evidence, and treats confirmed blank OCR as a usable observation that closes stale visual-hash urgency. + UX16d17 Build 539 adds exact production crop capture, bounded transaction tracking, known-session and adversarial replay gates across the real control-plane/publication/reducer/event path; clears stale scheduler priority, makes real confirmation transactional, rejects malformed timer evidence, limits event-free penalty baseline arming to eight seconds and keeps pending penalty popups valid through legitimate timer countdown; physical iPad validation remains mandatory. + UX16d18 Build 540 adds bounded active-penalty publication state, hard OCR service ceilings, a three-period goals/penalties timeline, integrated scorebug penalty/manpower presentation and nonblocking recording overlay startup. + UX16d19 Build 541 bounds unresolved penalty visual urgency, guarantees fair unresolved-slot audits, prevents unusable baseline reads creating pending work, labels the compact timeline with actual team names and event times, simplifies period headings, and shows a subtle OCR LIVE badge only while OCR is genuinely running. + UX16d20 Build 542 removes timeline event-time text, makes inactive unresolved penalties hash-driven after one sample, prioritises real paired confirmation after Clock freshness, and reduces continuous OCR load. + UX16d25 Build 548 consolidates Builds 543-547: one production OCRWorkScheduler, one OCREvidenceStore, a single event coordinator, player-owned penalties, hash-first goals, game-clock-correlated 2/4/5/10-minute penalty validation, physical player-zone clearing and architecture/replay gates. + UX16d27 Build 549 adds adaptive 0.70/1.25/1.50-second Clock service, retains hash-triggered score/player evidence while strict stopped-Clock confirmation completes, reserves atomic penalty player+timer work with a dedicated timer-mask budget, releases reducer-confirmed event popups immediately and removes the duplicate All Logs export action. + UX16d28 Build 550 replaces zero-median whole-zone hashing with background-relative active-foreground occupancy hashing, adds bounded stopped-window score/player safety transactions, yields Clock confirmation after two consecutive passes to real event work, and exports hash values/distances/threshold decisions. + UX16d29 Build 551 removes the separate stopped-Clock candidate publication gate, commits hash-opened goals after two matching trusted score reads, commits hash-opened penalties after two trusted player/timer reads, retains bounded transactions across Clock restart and removes late event-free penalty recovery. + UX16d34 Build 557 forces the complete Build 551 event runtime overwrite through CHANGED_FILES_ONLY, preventing stale Build 554/555 reducer and scheduler files from surviving an incremental install. + UX16d36 Build 558 enforces physical event latency, atomic penalty-pair service, compact two-penalty presentation, concurrent timeline lanes, landscape score zones and an earlier native launch brand. + UX16d37 Build 559 adds an independent Image Relay scoreboard mode: saved perspective-corrected field zones are converted into transparent live glyph images and rendered through the existing scorebug/recording template without Vision, ML Kit, character classification, numeric validation or MatchState publication; semantic goal and penalty cues remain explicit operator actions. + UX16d37a Build 560 fixes the Image Relay compile boundary by keeping imageRelayEnabled externally read-only and routing mode changes through a ViewModel-owned setter; it also includes the required Info.plist in the incremental package and removes the Swift concurrency isolation warning from the clip export priority contracts. + UX16d37b Build 561 exposes OCR, Image Relay and Manual selection directly in Calibration Controls -> OCR and adds a persistent compact mode menu beside the OCR-screen Start/Stop control, with mode-specific run labels and status text. + UX16d37c Build 562 fixes solid Image Relay blocks by applying the saved per-zone colour pipeline as a non-recognising pixel mask, rejecting full-zone/background masks and emitting valid premultiplied transparent glyph images for both live and recorded scorebugs. + UX16d37d Build 563 fixes frozen relay fallback and unstable glyph sizing with timer-aware masks, fixed canonical field canvases, reflection trimming, transient core-field retention, relay crop-padding support and mode-specific Calibration controls. + UX16d37e Build 564 makes the saved Raw zone the Image Relay geometry authority, replaces hard threshold/bounding-box/canonical-canvas/thickening stages with one soft full-zone colour-to-alpha pass, adds Raw-versus-Published calibration preview, and suppresses penalty timers when no player image is present. + UX16d37e1 Build 565 fixes the Calibration Raw-versus-Published preview closure compile error without changing Image Relay runtime behaviour. + UX16d37e2 Build 566 restores a self-contained asset catalogue by adding the required AppIcon and AccentColor sets directly to RinkLensLaunchAssets.xcassets, so no separate Assets.xcassets project reference is required. + UX16d37f Build 567 removes physical scoreboard frames from Image Relay, normalises soft glyphs to stable field canvases, rejects blank penalty dash fields, restores yellow period publication, and moves Raw relay previews off the MainActor OCR-preview path. + UX16d37g Build 568 isolates Clock Image Relay onto a dedicated fast lane with a 1.5-second stale-image expiry, keeps static fields on a separate bounded lane, preserves score glyphs at native crop resolution, sharpens alpha edges, uses stable perceptual hashes and disables high interpolation for score/period/penalty rendering. + UX16d37h Build 569 isolates dominant relay glyph components more aggressively, applies tighter per-field bounds, enlarges the period rendering, slightly increases score presentation size and adds revision-driven relay view refreshes so new clock/score images are more visibly reflected in the scorebug. + UX16d37i Build 570 replaces the relay Clock path with an unconditional 0.5-second direct publisher that bypasses hash/change gates, OCR validation, temporal logic, stale expiry and the static-field lane. + UX16d37j Build 571 removes the hidden OCR-screen lifecycle dependency by making Image Relay stage saved camera identities and start its scoreboard-camera branch directly from any route. + UX16d37k Build 572 binds every Image Relay revision directly to the Broadcast and Settings canonical compositor, adds published/requested/rendered/displayed revision evidence, and gives Home/Away scores a native-resolution direct colour-mask path matching the Clock clarity. + UX16d37l1 corrected Build 573 removes Clock hashing, restores unconditional 0.5-second Clock publication, retains the last complete overlay during redraw and recording startup, and normalises Score/Period glyphs into fixed large canvases without retaining oversized calibration-zone margins. + UX16d37m Build 574 keeps the direct no-hash Clock path at 0.30 seconds, separates Home/Away Score to 2.0-second service and Period to 5.0-second service, replaces live content-bound scaling with fixed perspective-corrected native cells, enlarges Score/Period presentation, redraws only the scorebug layer for Image Relay changes, prewarms recording overlays off-main and removes Record Match controls from the Recording tab. + UX16d37n Build 575 recognises Home Score, Away Score and Period only from their rectified Image Relay masks, confirms visual changes locally and renders stable upright seven-segment glyphs without publishing OCR, MatchState or event side effects. + UX16d37n1 Build 575 compile fix qualifies the static compact-diagnostic helper at instance call sites for Xcode Swift compilation without changing runtime behaviour. + UX16d37o Build 576 removes relay-mask OCR and custom seven-segment score/period images, reuses the original rectified crop, saved colour profile and OCR-mode dynamic-token parser, and renders confirmed visual-only Score and Period values through the same normal scorebug text paths used by OCR mode. + UX16d37p Build 577 keeps Clock and penalty timers as fixed direct Image Relay images, recognises penalty-player numbers through the OCR-mode parser and normal scorebug text style, keeps both penalty cells present while inactive, activates each slot after two matching player reads, clears it after three player misses, and deliberately does not use 0:00 detection. + UX16d37q Build 578 separates penalty work into a 2.00-second player-recognition lane and an active-only 0.30-second direct timer lane matching the main Clock cadence, preserving fixed timer geometry, inactive placeholders and player-owned clearing without 0:00 detection. + UX16d37r Build 579 makes penalty-player crops match the exact Calibration rectangle with no outward padding, invalidates player evidence when geometry changes, rejects contrast-only or undersized player artefacts, and prevents Penalty 2 from appearing until Penalty 1 is active for the same team. + UX16d37s Build 580 removes Image Relay crop padding from Clock and penalty timers, removes the added player-number hash prefix, accepts repeated trusted lower-confidence Slot 1 reads, and immediately refreshes a confirmed slot on a very strong replacement so Slot 2 is not blocked by a missing first-slot baseline. + UX16d37t Build 581 makes Command Centre a thermal-idle boundary by pausing OCR, Image Relay and an active recording, releasing the recording capture lease and stopping both camera branches; Broadcast/Recording rebind and resume the paused writer after a fresh capture generation. Penalty player text is enlarged and penalty timer relay images use detected illuminated glyph height to remove empty display margins and fill the slot. + UX16d37u Build 582 makes illuminated timer height authoritative at the final display boundary: timer masks are normalised onto a fixed 2.20:1 height-led canvas, the live and recording renderers reserve more timer width, player text fills the row without stealing timer height, and the last timer source/glyph/canvas measurements remain in All Logs after Command Centre idle. + UX16d37v Build 583 replaces the all-active-pixel timer bounds with non-recognising digit-band measurement: connected frame and rule components are removed, repeated row occupancy determines the timer digit height, sparse edge noise is trimmed, and the measured digit band is enlarged onto the existing height-led canvas with retained evidence in All Logs. + UX16d37w Build 584 gives penalty player and timer glyphs one shared final display height, applies the same non-recognising digit-band measurement and height-led normalisation to the main Clock, and replaces brittle consecutive penalty-player confirmation with a bounded rolling trusted-evidence window so intermittent weak/partial reads cannot suppress a repeatedly observed player such as 77. + UX16d37x Build 585 measures and preserves both width and height for Clock and penalty-timer digit groups, rescues outer Clock digits when component cleanup narrows the measured envelope, uses contain-fit with transparent safety margins, and matches penalty-timer visible height to the player font cap height rather than the full row container. + UX16d37y Build 586 fixes the scorebug Clock display zone to the operator-requested height:width ratio of 1:3.52 across live preview, recording and clips while retaining contain-fit and measured-glyph aspect preservation. + UX16d37z Build 587 changes the Clock zone to 1:4.60 and makes the configured team-name font the bounded master scale for Clock, score, logo, team-cell dimensions and the main scorebug panel while preserving period typography and penalty-row geometry. + UX16d38 Build 588 makes Clock and penalty-timer width authoritative from relay producer through live and recording presentation: minimum 4.60:1 transparent canvases, landscape centre cell, reserved two-slot timer width and contain-fit rendering with no crop or stretch. + UX16d39 Build 589 introduces one resolved scorebug metrics object with explicit output scale, one globally-clamped primary content scale, fixed period/penalty contracts, clarified desired-versus-fitting Clock APIs and a real SwiftUI/Core Graphics snapshot parity harness. + UX16d39a Build 590 fixes the SwiftUI penalty-strip frame overload and removes the snapshot harness default-argument MainActor isolation warning exposed by the first Xcode target build. + UX16d39b Build 591 makes the resolved-scorebug compile fix cumulative by replaying the defining metrics and recording-renderer dependencies in CHANGED_FILES_ONLY, preventing ScorebugView from being installed without BroadcastScorebugResolvedMetrics. + UX16d39c Build 592 fixes the remaining ScorebugView team-side scope compile failure by passing TeamSide explicitly into the justified and centred team-name helpers before they derive side-specific cell widths. + UX16d39d Build 593 unwraps UIImage.cgImage before passing it to the snapshot parity harness alpha-crop routine, correcting the final reported Xcode compile blocker without changing scorebug runtime behaviour. + UX16d39f Build 595 gives the main Clock and active penalty timers one shared width-preservation boundary before 4.60:1 contain-fit normalisation; penalty timers apply a stricter geometry-only bezel-edge filter while retaining complete outer digits, player-owned activation and no 0:00 clearing. + UX16d39g Build 596 locks each active penalty timer to one player-owned source-space anchor that expands but does not shrink or recenter, and derives visual-only manpower from confirmed relay player slots for live/recording parity. + UX16d39h Build 597 enlarges the centre manpower readout and gives Centred Above Name the same density-specific logo sizes as Beside Name. + UX16d39i Build 598 makes Compact/Expanded the only scorebug geometry authority, isolates Team Font to team-name text, holds the preferred larger Clock envelope without frame-to-frame breathing, suppresses penalty-timer glow blobs while filling pinholes, expands penalty-player crops safely and protects confirmed player numbers from weak replacements. + UX16d39j Build 599 compacts already-authorised surviving Slot 2 penalties into visual Slot 1 and separates stable timer width from the tight illuminated vertical digit band. + UX16d39k Build 600 removes Compact/Expanded from the operator UI and restores Team Font as the one bounded master scale for scorebug geometry and typography. + UX16d39l Build 601 keeps route-suspended recordings genuinely paused until explicit Resume, restores their capture lease after Broadcast returns, and verifies Team Font scaling for Period, Clock and visual manpower in live and recorded output. + UX16d39m Build 602 adds metadata-only raw Clock validation, automatic goal/penalty/strength popups and raw-time timeline events beside Image Relay without changing relay images, timer activation or MatchState. + UX16d39n Build 603 keeps automatic Image Relay goals provisional through the restart delay and cancels a pending goal when the confirmed score is corrected before popup, timeline or clip publication. + UX16d39o Build 604 fixes the Xcode compile boundary in raw Clock metadata by separating movement and validation state variables and making the pure tenths parser explicitly nonisolated. + UX16d39p Build 605 freezes popup time from a monotonic physical-stop Clock model, recovers missed restart transitions from validated raw movement, and publishes confirmed live penalty-expiry strength popups. + UX16d39q Build 606 gives Broadcast, OCR Setup, Recording, Diagnostics and Settings one fixed upper-left Command Centre return position and moves conflicting titles below the shared navigation clearance. + UX16d39r Build 607 preserves the established Image Relay cadences and separates penalty-player removal from noisy OCR text: three colour/geometry-confirmed blank player crops clear the player-owned slot, while parser artefacts cannot keep an expired player locked or block a later genuine replacement. + UX16d39t Build 609 fully restores the Build 607 Clock movement/restart implementation and retains only the penalty-player same-crop contradiction fix, so popup and timeline release timing is no longer held by Build 608 pulse-window logic. + UX16d39u Build 610 moves Clock movement authority onto every 0.30-second Image Relay crop, makes the first material digit change start running immediately, freezes stops from the first stable crop, keeps numeric Clock OCR secondary-only, and adds direct saving of current Calibration zones to the default profile. + UX16d39v Build 611 attaches the immutable first-stable Image Relay Clock image and stoppage identity to goal/penalty events, renders that image through the live Clock component in SwiftUI and recording popups, keeps numeric time unresolved when stopped OCR fails, positions unresolved timeline events from the next trusted OCR anchor, delays live penalty-removal strength popups for two-second revalidation, and calculates the resulting manpower message without changing live scorebug variables. + UX16d39w Build 612 adds a permanently visible Calibration SAVE ZONES control with named-profile confirmation, requires a true 1.20-second Clock-stability duration against the first stable signature, atomically rebinds penalty players across both physical slots, accepts only repeated geometry-trusted colour evidence when colour and contrast disagree, and exports complete penalty-cycle and logical reconciliation evidence without changing the displayed scorebug variables. + UX16d39x Build 613 opens Calibration with the saved zones visible, arms OCR/Image Relay mode changes in a stopped state for explicit operator start, and makes the Image Relay control read authoritative pause state immediately so Stop changes to a green Resume Relay button without waiting for the sampled Calibration runtime. + UX16d39y Build 614 records confirmed goals and player-owned penalty starts/ends to the timeline independently from popup eligibility, preserves active penalty identities through route and capture-generation changes, rejects stale pre-resume metadata, prevents transition-wide blank crops from clearing all penalties, uses identity-aware slot rebinds and timeline bars, and exports a complete timeline event ledger without changing scorebug variable presentation. + UX16d39z Build 615 preserves the Slot 2 anti-ghosting gate but authorises it from physical Slot 1 occupancy, carries complete slot occupancy/identity evidence into team-level lifecycle reconciliation, confirms rebinds before three-snapshot absence removal, keeps estimated Clock values diagnostic-only, and rejects unattended score jumps in both Image Relay visual and metadata paths. + UX16d39z1 Build 616 fixes the Xcode memberwise-initializer boundary so penalty-slot evidence is actually passed into metadata observations, and marks pure score/period clamp helpers nonisolated for the target-wide MainActor default without changing Build 615 runtime behaviour. + UX16d39z2 Build 617 makes penalty-removal proof non-consecutive and per-team: unresolved crops pause evidence, contradictory complete snapshots reset it, stale candidates expire after 20 seconds, and one team no longer depends on the other team's crop completeness. + UX16d39z3 Build 618 restores scorebug-first Image Relay output, persistent scores/logos/timers, delayed popups and removes the bottom timeline. + UX16d39z4 Build 619 adds per-slot verified blank baselines, 0.30-second penalty-player hashing, change-triggered OCR, physical-occupancy timer activation and atomic [45,56] to [56,77] shift-and-fill reconciliation without reconnecting timeline code. + UX16d39z5 Build 620 fixes the Xcode scope failure in the occupancy-activated penalty-timer lane and preserves timer relay for physically occupied slots before player OCR resolves. + UX16d39z6 Build 621 removes OCR as an operator mode, makes penalty-player scorebug fields Image Relay-only, bounds stable-crop recognition for Home roster popup names, keeps Guest popup numbers image-only, retains internal Period recognition, fixes full-crop blank occupancy evidence, widens penalty-timer source safety and enlarges popup Period presentation. + UX16d39z7 Build 622 clears the Xcode Swift 6 concurrency warnings exposed by the first full Build 621 target compile, removes the deprecated clip-export preset query and preserves Build 621 runtime behaviour. + UX16d39z8 Build 623 removes stale OCR-era player-text diagnostics from the Image Relay penalty-timer lane and reports physical relay identity without undefined local variables. + UX16d39z9 Build 624 makes the 0.30-second penalty timer lane the unconditional viewer-facing authority for both player and timer Image Relay crops, removes the player-occupancy activation gate, holds one or two weak frames and clears the physical pair only after three consecutive timer-zone misses. + UX16d39z10 Build 625 treats the physical scoreboard dash/dot pattern as an empty penalty slot, extracts and scales only tall player-number glyphs, activates each direct player/timer relay pair from the player glyph rather than timer placeholders, and restores placeholder-to-player lifecycle evidence for penalty popups. + UX16d39z11 Build 626 restores automatic Image Relay goal popups, retains automatic penalty popups, keeps score changes provisional until restart release, and leaves Manual controls as corrections rather than the sole event source. + UX16d39z12 Build 627 restores the last selected team and calibrated rink profiles before Broadcast, makes stable physical player transitions authoritative for penalty starts, hardens restart-plus-five popup timing against false stops, keeps unresolved event times non-numeric, dynamically widens player glyphs, applies team score colours and debounces Settings preview, cache invalidation and persistence. + UX16d39z13 Build 628 gives Clock, penalty timer and player images one shared visible-glyph resolver and colour authority across live Broadcast, recording, clips and Settings; auto-saves active team profiles, renders Settings previews off-main from a frozen relay revision, keeps restart-plus-five deadlines immutable, restores only explicitly active/default/calibrated rink profiles and adds end-to-end relay latency telemetry. + UX16d39z14 Build 629 resolves the Xcode frontend diagnostic failure in popup-release telemetry by decomposing the nested audit expression into explicitly typed local values and clears the relay metadata-only constant warnings without changing Build 628 runtime behaviour. + UX16d39z15 Build 630 right-aligns Home and left-aligns Guest names through one shared mirrored alignment contract, reserves a measured full-size two-digit score column and removes score shrinking across live Broadcast, recording, clips and Settings preview. + UX16d39z16 Build 631 restricts production OCR to scores, Period and penalty-player numbers; removes Clock and penalty-timer OCR scheduling; adds ±1 score acceptance, physical score fallback, stoppage-baseline popup authority, manual large-jump confirmation and a retained-overlay fast recording start that does not disturb Broadcast. + UX16d39z17 Build 632 restores the Clock and every grouped penalty player/timer calibration zone required by Image Relay, separates calibration/Image Relay geometry from production OCR authority, and preserves all Build 631 score, popup and fast-recording behaviour. + UX16d39z19 Build 634 restores automatic external scoreboard-camera recovery, strict placeholder-safe player relay, independent player/timer publication, score confirmation delivery and shared measured glyph sizing. + UX16d39z20 Build 635 stabilises penalty lifecycles, popup timing, large-score confirmation, measured glyph layout and recording cadence from the 2026-07-18 physical game review. + UX16d39z21 Build 636 deduplicates goals permanently per stoppage, preserves simultaneous same-number penalties, promotes strong current player OCR over stale retained identity during slot shifts, uses normalised source canvases for equal visible penalty height, enlarges popup time and reduces recording duplicate-frame work. + UX16d39z22 Build 637 fixes the full Xcode target compile failures by exposing Image Relay diagnostics through an explicit HockeyScoreboardViewModel extension and removing MainActor-isolated function references from MatchState reducer Optional.map operations. + UX16d39z23 Build 638 makes the Image Relay submit interface backward-compatible, explicitly delivers its timing dependency with partial updates, and removes the unused penalty physical-hash binding exposed by the full Xcode target compile. + UX16d39z24 Build 639 restores target compile parity. + UX16d39z25 Build 640 measures visible penalty glyph height independently of the outer zone canvas. + UX16d39z26 Build 641 moves Home/Guest penalties into mirrored side panels, reduces Clock-centre padding, isolates player digits from box labels/frames, rejects stale penalty metadata and Clock movement frames, requires more than two seconds of stable Clock pixels before declaring a stoppage, and prevents occupied-slot OCR corrections from creating duplicate lifecycles or late invalid popups. + UX16d39z27 Build 642 corrects the SwiftUI team-cell frame overload exposed by the full Xcode target build without changing Build 641 runtime layout or event behaviour. + UX16d39z28 Build 643 hides every unconfirmed blank-slot glyph and timer from the scorebug/manpower calculation, freezes stable player images for penalty events, supports OCR-free penalty popup creation, binds early physical penalties to the confirmed stoppage, preserves two same-team penalties in one stoppage and prevents Image Relay revisions from starving popup redraws. + UX16d39z29 Build 644 treats player displays as multiplexed LED sources, confirms occupancy from two illuminated observations without resetting on dark scan frames, requires sustained blank evidence before clearing, and renders live popups independently from the retained recording compositor. + UX16d39z30 Build 645 scales live popups in the canonical 1920x1080 coordinate space, streamlines penalty and strength banners, suppresses intermediate 5v4 removal popups, hides all inactive timer placeholders and rescues mature goal events orphaned by a false stoppage split. + UX16d39z31 Build 647 corrects the live streamlined penalty sponsor logo decode to use SponsorResolvedBroadcastSponsor.logoData, resolving the full Xcode target compile failure without changing Build 645 runtime layout or event behaviour. + UX16d39z34 Build 649 removes the absolute vertical-centre rejection introduced in Build 647 so strong repeated player glyphs activate independently in all four physical slots while blank-slot presentation remains unchanged. + UX16d39z35 Build 650 resets the penalty path to calibrated-box authority: signal density plus measured height determines occupancy, two occupied observations activate regardless of multiplex hash, Slot 2 is independent, fixed player canvases stop height breathing, and physical activation directly authorises timeline and popup creation. + UX16d39z36 Build 651 preserves Build 650 runtime behaviour and corrects the Xcode installation state by removing duplicate suffixed source files before replacing the canonical project files. + UX16d39z37 Build 652 requires matching physical evidence from the calibrated player and timer boxes before viewer output, manpower, lifecycle or popup activation; isolated player-box fragments remain inactive. + UX16d39z38 Build 653 removes Camera, OCR and Recording as separate operator diagnostics pages, exposes only Match Day and Engineering logging modes, and retains all subsystem evidence inside Support Logs without changing Build 652 penalty runtime behaviour. + UX16d39z39 Build 654 gives player/timer glyphs one measured visible height across live and recorded scorebugs, clears a confirmed slot from sustained timer absence, coalesces same-team same-stoppage penalty popups to the first thin popup, and widens team cells for longer names. + UX16d39z40 Build 655 removes same-stoppage popup coalescing and forwards every valid goal and penalty to one sequential overlay queue so later events wait for the active thin popup to finish. + UX16d39z41 Build 656 restores the shared Clock-style digit relay for every penalty timer, removes timer-specific red-ring and historical-width presentation from the live path, and fixes one visible penalty height across all four player/timer pairs in live and retained outputs. + UX16d39z42 Build 657 prevents the recording cache from supplying a popup-bearing composite to the live preview, leaving the thin SwiftUI banner as the single preview popup while recording and preserving one independently rendered popup in saved output. + UX16d39z43 Build 658 renders Settings overlays on their first pass and keys logo changes, places recognised Home player names immediately left of the penalty player number, fixes player/timer relay canvases and presentation identity to stop size pulsing, and aligns independent blank clearing to five sustained observations. + UX16d39z44 Build 659 debounces Settings preview rendering and team-name persistence, removes automatic Camera-page discovery, prewarms recording folders, delays periodic recording overlay refresh and blocks recording profiles above the active Broadcast source cadence. + UX16d39z45 Build 660 makes the last complete popup-free overlay the sole presentation authority, renders replacements asynchronously, coalesces all scorebug setting/profile/sponsor changes and swaps overlays atomically only after successful completion. + UX16d39z46 Build 661 restores activation-owned penalty visual geometry: frozen player images, two-frame timer-anchor acquisition, no shrink/recentre, confirmed expansion only, meaningful pixel hashes and session-stable Clock publication. + UX16d39z47 Build 662 removes player/timer box rules from viewer glyphs, excludes contaminated timer evidence from pair authority, lets sustained clean-negative evidence clear Slot 1 so the existing Slot 2 compaction runs, and holds trusted score text through transient parser disagreement instead of showing a donut-like physical zero. + UX16d39z48 Build 663 adds Guided Calibration with a live precision loupe, persistent per-zone locks, one/five-pixel movement and sizing, live focus/exposure/contrast/zone-fit scoring, and orientation guidance without changing camera zoom or source coordinates; it also preserves penalty occupancy/visual identity across Broadcast/OCR capture hand-offs and republishes persisted team logos whenever Broadcast remounts. + UX16d39z49 Build 664 makes the Calibration preview, four-corner scoreboard frame, visible zones, OCR crops, Image Relay crops and per-zone colour sampling share one projective coordinate model; adds inline whole-screen focus/exposure controls and visible-character colour detection with field defaults. + UX16d39z50 Build 665 makes all four Screen Align corners modal and directly draggable, removes the duplicate Focus/Light panel, maps the Guided Calibration loupe with aspect-fit geometry so its box matches the shown pixels, moves colour picking into the zoomed guide, and evaluates manual exposure as Too Dark, Good or Too Bright from illuminated scoreboard content inside the aligned screen polygon. + UX16d39z51 Build 666 removes the full-frame drag hit layer that intercepted alignment controls, gives TL/TR/BR/BL independent translation-based touch targets, moves whole-frame movement to a dedicated centre handle, detaches Reset/Done from the camera canvas, and normalises Guided Calibration zoom, colour and lock buttons to one compact style. + UX16d39z52 Build 667 adds detached TL/TR/BR/BL corner selectors, makes the selected corner orange and yellow while moving, and lets the operator drag anywhere on the camera image to move only the selected corner. + UX16d39z53 Build 668 makes Guided Calibration a persistent floating panel with a dedicated drag handle and constrains Screen Align to a true parallelogram so both pairs of opposing sides remain parallel while any selected corner is moved. + UX16d39z54 Build 669 uses the saved Screen Align parallelogram as an affine coordinate basis, deskewing the complete scoreboard to an upright canonical image before Guided Calibration, OCR, colour sampling, visual hashing or Image Relay crops are produced. + UX16d39z55 Build 670 gives all four Screen Align corners independent movement, applies full projective upright correction to Guide/OCR/Image Relay, adds manual per-zone colour sampling with visible swatches and completion marks, shows detected character top/bottom/width, and retains displayed penalties across route-only OCR suspension. + UX16d39z56 Build 671 seeds Home/Guest score baselines independently, keeps clean numeric score text during confirmation, and lets confirmed player-zone blanks clear stale penalty lifecycles even when timer artefacts remain. + UX16d39z57 Build 672 aligns the Guided Calibration fitted image, exact OCR rectangle and rectified character bounds, adds safe left/right preview gutters and keeps all evidence labels inside the selected zone. + UX16d39z58 Build 673 replaces operator-facing hex text with a large actual-colour swatch and clear selected-colour/calibration labels. + UX16d39z59 Build 674 restores stable Broadcast logo presentation, reuses the proven live zoom path for preset buttons, processes score evidence during stopped-clock candidates, rejects automatic score decreases, tightens 1-versus-7 recognition, keeps manual score adjustments event-free, preserves the unchanged score, equalises penalty-timer height and expands Clock/popup geometry for sub-second digits. + UX16d39z60 Build 675 saves automatic/manual colour calibration with Save Zones, replaces flashing system OCR menus with wider non-wrapping popovers, confines Guide quality text inside the floating panel and immediately refreshes exact upright character bounds when zones change. + UX16d39z61 Build 676 routes Broadcast zoom to the CaptureEngine-owned camera after route changes, retains complete scorebug/logo state across remounts, restores atomic Slot 2-to-Slot 1 penalty compaction, freezes penalty sponsor/player identity, offers explicit downward score correction, holds fixed Clock/timer geometry, adds an 80% paused-recording veil, counts actual media files and expands exact Guide character evidence so it cannot cut visible strokes. + UX16d39z62 Build 677 gives the score lane a direct metadata path, forces occupied Slot 1 recognition when Slot 2 clears, defers capture restarts while backgrounded, restores automatic camera quality after 0.5x zoom, hides Image Relay calibration UI in Manual mode, rejects textless Settings overlays and centres all penalty timer digit bands on the same canvas anchor. + UX16d39z63 Build 678 treats a blank Slot 2 as an accelerator rather than a requirement, forcing current-crop recognition on material identity changes in either slot so [21,45] can reconcile to either [45,blank] or [45,77] without losing the continuing lifecycle. + UX16d39z64 Build 679 fixes the Build 678 Xcode compile boundary: Image Relay resume protection is visible to the cross-file ViewModel extension, centred relay CGRect dimensions are CGFloat, and the reported unused/non-mutated locals are removed. + UX16d39z65 Build 680 restores Guest 4 recognition through extended sequential evidence, locks Clock geometry across capture generations, removes per-frame penalty-timer recentering and preserves repeated cross-slot player identity during compaction. + UX16d39z66 Build 681 makes new penalty lifecycles transactional and one-owner-per-slot, reduces main-thread heartbeat publication pressure, fills the requested recording cadence with bounded repeated source frames while reporting native source FPS honestly, and retries external OCR reconnects across bounded recovery waves without a failed-contract cooldown. + UX16d39z67 Build 682 prioritises the virtual/ultra-wide Back source for working 0.5x zoom, replaces remaining live system menus with stable fixed-width popovers, keeps operator controls above presentation overlays, coalesces Settings preview renders without cancellation starvation, detaches recording prewarm from the live overlay cache, locks timer character centres, restores Clock readability and resolves penalty sponsors from the immutable event player. + UX16d39z68 Build 683 restores the overlay cache key at its canonical render entry point and removes the now-unused detached-prewarm key without changing runtime policy. + UX16d39z69 Build 684 publishes confirmed penalties through a fixed provisional timer anchor before strict geometry lock, reports exact timer-rejection reasons, requires identity-specific proof for new lifecycles, forces unresolved occupied-player verification, cancels false restart deadlines, holds operator popups while Broadcast is hidden and transactionally clears the served penalty after a power-play goal. + UX16d39z70 Build 685 recovers repeated high-confidence first-goal 0-to-1 score evidence without weakening general mask-disagreement protection, and scrubs penalty-timer frame rows/columns before measurement and publication so failed digit-band detection cannot relay the complete boxed zone. + UX16d39z71 Build 686 corrects the Build 685 Clock compile boundary, removes complete-zone fallback from both Clock and all four penalty timers, holds the last clean relay image when no safe envelope exists, and records the all-zone relay audit. + UX16d39z72 Build 687 reconstructs golden relay and match-state ordering: sequential current-plus-one score conflict recovery, strict clean timer authority with no provisional/full-zone path, baseline-aware prompt penalty removal, physical-transition penalty admission when Clock metadata is unavailable, restored secondary Clock metadata and uninterrupted full-match recording across operator routes. + UX16d39z73 Build 688 stabilises the complete Broadcast Setup scorebug editor: removes root AnyView replacement and lazy-scroll invalidation, keeps active-profile autosave structurally silent, scopes profile refresh identity to Profiles only, and prevents Team Font, layout, weight, colour, visibility or reset edits from leaving a blank Settings screen. + UX16d39z74 Build 689 restores prompt physical penalty publication when a stable player has one strict timer sample, repairs the unreachable non-OCR lifecycle gate, freezes the first exact stopped-clock time for the popup, and fixes Guided Calibration at 8× without zoom buttons. + UX16d39z75 Build 690 establishes explicit state ownership for match, penalty, popup, manual-score and camera-zoom domains; removes writable ViewModel mirrors; adds temporary rollback flags; and records structured previous/new/source/reason transitions in all operating modes. + UX16d39z76 Build 691 makes team identity and logos transactional, prevents OCR from overwriting team names, declares scorebug settings as the appearance owner with structured mutations, and separates accepted Clock state from relay metadata, trusted anchor and presentation projections. + UX16d39z77 Build 692 centralises calibration/profile, OCR configuration, complete camera-control intent, event lifecycle, route/capture and match-journal state; freezes popup policy per event; protects sponsor/roster mutations; and adds structured navigation and streaming transitions. + UX16d39z78 Build 693 fixes the Xcode ownership boundary by routing exact-clock, estimated-clock, stoppage reassignment, undo and clear mutations through RinkLensMatchEventJournal instead of mutating its read-only timeline projection. + UX16d39z79 Build 694 makes ScoreboardImageRelayStore the one viewer-facing physical scoreboard authority, retains its last valid clock/score/penalty snapshot while processing is paused, and adds a feature-flagged scoreboard-input lifecycle owner with structured source-selection logging. + UX16d39z80 Build 695 compacts each penalty side panel by about one third, admits strong edge-filled 10:00/2:00 timer groups behind a rollback flag, atomically rebinds a continuing physical penalty identity during slot compaction, and records pair admission/rebind transitions with previous/new/source/reason evidence. + UX16d39z81 Build 696 crops relay safety canvas at the shared live/recording display boundary so penalty timers match player-number visible height, verifies two real Clock digit changes before starting popup restart timing, creates a provisional goal event from an accepted +1 score inside the stopped-board transaction, and preserves released-after-restart lifecycle evidence through the popup queue. + UX16d39z82 Build 697 restores stable fixed penalty-timer canvases with a single 1.08 display scale, enlarges Period and strength text without changing their bands, admits proven physical penalty pairs promptly and binds late-confirmed pairs to the preceding verified restart, and prevents OCR dead-branch recovery from mutating capture while recording. + UX16d39z83 Build 698 makes BroadcastOverlayState the one viewer-scoreboard snapshot owner, removes Image Relay metadata from the display path, derives Image Relay enabled state from the input lifecycle, logs every visible field value/source transition, and reports overdue feature flags without deleting unaccepted rollback paths. + UX16d39z84 Build 699 enforces renderer, pending-event, camera-profile, OCR-evidence, defaults and atomic-settings ownership with active feature-flag governance and exact viewer-source logging. + UX16d39z85 Build 700 corrects the Xcode renderer-migration compile boundary without reopening any second state or direct relay-store input. + UX16d39z86 Build 701 completes the recording-overlay viewer-snapshot migration by deriving cache state from the sole snapshot, propagating relay data through explicit renderer parameters and removing stale cache/relay arguments reported by Xcode. + UX16d39z87 Build 702 gives the event lifecycle one canonical semantic identity and one verified-running score boundary, suppresses stale power-play penalty re-entry, accelerates only proven blank-to-occupied penalty pairs and logs every latency checkpoint. + UX16d39z88 Build 703 removes player extraction from the fast penalty-timer lane, fixes all timer slots to one illuminated height and activation-owned centre, binds fresh Clock evidence to one stoppage owner and logs timer frame age, processing and publication cadence. + UX16d39z89 Build 704 widens the Clock crop without growing the canvas, enlarges centre text behind a rollout flag, prefers non-binned internal formats and adds physical compaction evidence. + UX16d39z90 Build 705 makes centre typography unconditional, restores stop-candidate Clock evidence, fixes verified Slot 2-to-Slot 1 handoff, returns internal Auto to the camera device native format and reduces timer-lane work. + UX16d39z91 Build 706 enforces one writable owner per state domain, makes Broadcast/recording entry read-only, consolidates penalty compaction into one atomic transaction, separates requested camera controls from hardware acknowledgements, formalises popup ownership and adds complete owner/transaction structured logging. + UX16d39z92 Build 707 makes the verified Broadcast camera source the sole authority for recording dimensions and cadence, removes active recording resolution/FPS selectors, freezes one immutable source snapshot per session and records all preflight/profile/state transitions. + UX16d39z93 Build 708 retires the viewer event timeline and removes its secondary textual Clock OCR queue, ten-second sampling and recovery bursts while preserving frozen Image Relay Clock evidence and the internal event journal. + UX16d39z94 Build 709 removes the two stale manual-Broadcast references to the retired textual Clock metadata owner, retaining accepted match-state text for Manual seeding and physical Image Relay Clock evidence for display. + UX16d39z95 Build 710 makes the production diagnostic helper resilient to stale numbered source copies by moving active call sites to one uniquely named canonical type, without changing recording behaviour. + UX16d39z96 Build 711 makes camera feature-policy reads queue-safe and routes telemetry/recording-format diagnostic publication through explicit MainActor bridges. + UX16d39z97 Build 712 completes semantic compile coverage for feature flags and nonisolated structured logging value types. + UX16d39z98 Build 713 repairs Image Relay return contracts and queue-safe timer-geometry admission after Clock timeline OCR removal. + UX16d39z99 Build 714 makes essential state-transition logging and ownership assessment mandatory, formally reviews every rollout flag, and retains unaccepted behaviour rollbacks through Build 720 with explicit evidence. + UX16d40a00 Build 715 pauses recording on Broadcast route loss, widens timeout Clock evidence, locks penalty-timer horizontal geometry, suppresses OCR calibration feedback loops, removes obsolete Logs cards and resolves supported built-in Auto to source-owned 1080p60. + UX16d40a01 Build 716 makes AppCoordinator the sole visible-route owner, consolidates Broadcast route-loss recording policy into one controller and retains the previous continuous-recording behaviour behind a governed rollback flag. + UX16d40a02 Build 717 restores direct OCR preview tap-to-Manual behaviour through the authoritative mode owner and governs the shortcut with a rollback flag. + UX16d40a03 Build 718 creates penalty lifecycles from newly visible confirmed physical player/timer pairs when zone misalignment prevented a blank baseline, and treats OCR as later identity enrichment only. + UX16d40a04 Build 719 fills the existing Clock canvas without transparent side gutters, enlarges the Period glyph, requires visible physical penalty proof for power-play goals, preserves Image Relay across route changes and clears stale manual camera profiles when built-in Auto is selected. + UX16d40a05 Build 720 makes Settings camera-selection-only, gives Broadcast a role-owned 1080p60 automatic default and OCR a role-owned 1080p30 automatic default, and moves exact format and capability-aware lens overrides onto their consuming operational screens. + UX16d40a06 Build 721 corrects the optional applied-cadence diagnostic expression and removes a retired unused penalty snapshot variable without changing camera or penalty behaviour. + UX16d40a07 Build 722 corrects the OCR preview Manual-mode snapshot refresh to use the calibration runtime injected into the top overlay, removing the unresolved parent-scope reference without changing camera, OCR or penalty policy. + UX16d40a08 Build 723 retains completed event identities, binds power-play goals to confirmed physical penalty removals, restores direct Broadcast camera controls and simplifies operator Engineering diagnostics. + UX16d40a09 Build 724 corrects the feature-definition array delimiter exposed by the full Xcode Simulator build without changing Build 723 runtime behaviour. + UX16d40a10 Build 725 combines Broadcast camera and recording controls, removes duplicated operator validation panels, hides Game Clock Direction, opens Operator Controls at full height and simplifies the OCR camera sheet behind a rollback flag. + UX16d40a11 Build 726 removes Camera and Recording from Operator Controls, matches the Broadcast Setup capsule navigation style, moves codec/bitrate/stabilisation to Broadcast Setup > System, and makes Broadcast stabilisation an applied camera-control setting while OCR stays unstabilised. + UX16d40a12 Build 727 adds an original Broadcast Camera menu with recording safeguards, four configurable framing buttons, hardware capture-button commands, torch, selective hold-to-lock controls and preview-only grid/horizon guides through the existing camera and recording owners. + UX16d40a13 Build 728 adds a governed managed/custom recording output profile with codec, six capability-checked resolution/cadence modes, exact Mbps bitrate, storage estimates and source-capability preflight. + UX16d40a14 Build 729 replaces the unavailable VideoToolbox HEVC probe with a cached AVAssetWriter capability check without changing recording ownership, operator layout or rollout policy. + UX16d40a15 Build 730 removes SDK-ambiguous initializer method references, uses explicit typed conversion closures and adds a package validation gate to prevent the same full-compiler failure class recurring. + UX16d40a16 Build 731 rejects sunlight/exposure-driven false penalty occupancy with strong-pixel and alpha gates, stricter no-baseline pair confirmation and structured ambient-light evidence. + UX16d40a17 Build 732 adds a governed Broadcast image-quality policy, sustained high-ISO 60-to-30fps adaptation, physical Ultra Wide 0.5x at truthful 1080p30, landscape-correct horizon geometry, requested/supported/applied stabilisation evidence, started-game score baseline preservation, pre-baseline penalty popup rejection, no-op transition suppression and structured logging-health evidence. + UX16d40a18 Build 733 reconciles recording output to verified camera-source truth, coalesces and proves 0.5x rear-zoom application, and requires a material stopped-board score glyph transition before a running-clock candidate can become a goal. + UX16d40a19 Build 734 simplifies Calibration Controls to Broadcast-style capsule pages with one shared zone selector, removes Colours duplication from Recognition, and suppresses transient Clock glyph overlap at both the relay publication and SwiftUI presentation boundaries. + UX16d40a20 Build 735 makes automatic colour-pipeline selection authoritative per zone, connects the Colours toggle to runtime resolution, and gives Guided Calibration Auto and Pick Colour distinct predictable ownership. + UX16d40a21 Build 736 preserves complete penalty-player glyphs, rejects unconfirmed rapid-timeout Clock overlaps, corrects oldest-penalty power-play cancellation and Slot 2 compaction, moves Players to Squad, consolidates storage and clips in Media, and restores an operator-only event timeline. + UX16d40a22 Build 737 coalesces penalty processing and removes colour-calibration UI stalls. + UX16d40a23 Build 738 freezes open-writer Broadcast cadence and adds bounded source holdover with structured contract evidence. + UX16d40a24 Build 739 moves the SponsorCatalogueStore-owned roster to a dedicated Squad tab beside Event Popups, keeps the nested Sponsor Squad route behind a rollback flag, removes the oversized embedded Sponsor minimum, and derives cached sponsor thumbnails off-main with structured cache lifecycle evidence. + UX16d40a25 Build 740 completes exhaustive feature-policy coverage for both Build 739 flags and adds a semantic package gate that compares every risk-feature enum case with policy and definition coverage without changing runtime behaviour. + UX16d40a26 Build 741 recovers repeated edge-filled penalty timers, isolates the Clock relay fast lane, preserves rear-camera recording through OCR disconnects, makes 0.5x exact and truthful, keeps Calibration overlays persistently mounted, moves Media indexing off-main, bounds derived rendering caches and throttles Engineering diagnostics during recording. + UX16d40a27 Build 742 corrects the CaptureEngine snapshot logging member and parenthesises the penalty-player Optional map exposed by the full Xcode target compile without changing runtime behaviour. + UX16d40a28 Build 743 serialises Image Relay Start/Stop through the lifecycle owner without blocking MainActor, derives both OCR controls from one stable presentation projection, restores upright cached overlay composition, rejects sustained black recording input and preserves typed recording stop origins. + UX16d40a29 Build 744 prioritises live relay presentation, uses immutable pre-removal penalty compaction evidence and protects the recording Broadcast generation from OCR-only USB notifications. + UX16d40a30 Build 745 suppresses nonessential presentation work while recording, renders overlays only for material pixel changes, shares relay rectification work, retries only the OCR output branch once per second without changing Broadcast generation, and makes the selected recording dimensions/cadence the exact Broadcast capture contract while preserving an independent OCR format. + UX16d40a31 Build 746 corrects every structured-event logger argument-order mismatch found by the full Xcode target compile and package-wide source audit without changing Build 745 runtime behaviour, capture ownership, recording quality, OCR independence or feature-policy rollback paths. + UX16d40a32 Build 747 gives the Clock a monotonic 0.30-second capacity-one scheduler, restores high-resolution aspect-preserving digit presentation, records truthful penalty-end and timeout semantics, and blocks nominal 60fps recording until measured Broadcast callbacks prove native cadence. + UX16d40a33 Build 748 removes duplicated Recording Profile and Camera & Recording cards from Broadcast Setup > System while retaining the previous layout behind a governed rollback flag; recording and camera authorities remain unchanged. + UX16d40a34 Build 749 gives Teams & Logos and Scorebug one shared canonical Live Broadcast Preview geometry without changing the underlying renderer, layout state or profile ownership. + UX16d40a35 Build 750 removes the redundant Scoreboard Input heading from the OCR-screen Image Relay/Manual popover while preserving the same compact control, actions and authoritative scoreboard-input lifecycle. + UX16d40a36 Build 751 hardens the titleless OCR input menu at both caller and shared-menu boundaries and locks the accepted presentation against stale persisted rollback values. + UX16d40a37 Build 752 replaces the OCR-screen Image Relay selector popover with a direct IMAGE-to-Manual and MANUAL-to-armed-Image-Relay toggle through the authoritative mode owner. + UX16d40a38 Build 753 makes RinkLensScoreboardInputLifecycleStore the sole selected-mode and run-state owner, removes active manual-protection mode inference and records each mode transition atomically. + UX16d40a39 Build 754 deletes the superseded scoreboard-input flags, selector path, manual-derived mode path, duplicate run/pause fields and legacy running-state mirror so the lifecycle store is unconditional. + UX16d40a40 Build 755 makes the Broadcast camera menu a 390-point popover and toggles selected physical camera locks directly from AVCaptureDevice state on successive preview holds. + UX16d40a41 Build 756 centres the 390-point Broadcast camera menu, removes device capture-button interception, and routes Broadcast focus, exposure, white-balance and torch mutations exclusively through HockeyCameraService with structured success and failure results. + UX16d40a42 Build 757 moves Broadcast camera selection into Camera & Recording, makes managed recording inherit the verified native camera source, preserves explicit custom-mode validation, prioritises the requested Ultra Wide device for 0.5x, admits repeatedly verified edge-filled penalty timers, removes fractional popup scaling and stops automatic decorative OCR preview work from blocking operator taps. + UX16d40a43 Build 758 exposes Scoreboard camera source selection in Camera Control and Scoreboard Setup, removes recording action controls from Camera Control, commits pinch resizing once through the calibration owner and sizes Clock relay from the detected character band rather than the outer zone. + UX16d40a44 Build 759 removes the duplicate requested/published logical camera-source values, makes HockeyCameraService.selectedCameraID the sole source-selection state and records structured source transitions while leaving Calibration, relay sizing and recording behaviour unchanged. + UX16d40a45 Build 760 corrects the CaptureSelectionSnapshot initializer label order and freezes the camera hold result before MainActor delivery, resolving the Xcode target errors without changing camera ownership or runtime policy. + UX16d40a46 Build 761 restores two-finger zone resizing on the perspective-projected Calibration path, reuses one board-space pinch resolver for both Calibration overlays and commits one final geometry change through RinkLensCalibrationStore. + UX16d40a47 Build 762 makes flat and perspective zone move, resize, rotation, pinch and penalty-group edits local drafts until one final atomic RinkLensCalibrationStore commit, with exact structured previous/new transaction events. + UX16d40a48 Build 763 makes the selected penalty group the exclusive gesture owner while Group mode is active, keeping player/time child zones visible but non-interactive on the perspective path so they cannot switch the editor back to Single. + UX16d40a49 Build 764 makes the perspective penalty-group overlay own both child-zone presentation and movement so the player-number and penalty-time zones visibly translate together with the group draft before one atomic commit. + UX16d40a50 Build 765 restores controlled 0.5x recording handoff, removes zoom-indicator state/UI, makes measured Clock glyphs own the output canvas, projects proven physical penalty compaction immediately and isolates live preview rendering from recording redraw work. + UX16d40a51 Build 766 binds one direct OCR latest-frame consumer to the authoritative FrameHub and removes continuous relay dependence on the retired camera-service callback. + UX16d40a52 Build 767 keeps the recording state and file authoritative during 0.5x lens handoff, retargets the existing writer capture contract to the verified graph and starts recording from the active Ultra Wide source without forcing a conflicting cadence. + UX16d40a53 Build 768 keeps External Camera as a permanent logical choice, separates physical availability from selected source, prevents rink-profile and discovery fallback from replacing the owner selection, and records availability transitions. + UX16d40a54 Build 769 makes build identity fail-loud during incremental installation by moving the sole RinkLensBuildInfo owner to a new canonical source filename, deleting the retired filename and exporting a derived source identity witness. + UX16d40a55 Build 770 makes 0.5x Ultra Wide an exact 1080p30 contract and changes the existing recording writer pacing and health cadence 60-to-30-to-60 in one file. + UX16d40a56 Build 771 makes the first 0.5x press authoritative by owning one pending lens transaction, preventing recording-lease route replay and stale generation zoom reapply, and accepting Apple-resolved equivalent Wide constituents. + UX16d40a57 Build 772 moves the complete physical lens, cadence, fresh-frame, recording-writer and rollback transaction behind CaptureLifecycleController, leaving the ViewModel as a request and status projection only. + UX16d40a58 Build 773 removes the stale capability-profile race, chooses one MultiCam-compatible rear input for both 0.5x and 1.0x, and applies framing/cadence in place without ordinary graph rebuilds. + UX16d40a59 Build 774 makes queued Broadcast zoom latest-request-wins, acknowledges every successful digital zoom, prevents stale 1x coalescing after 3x/5x, removes duplicate Image Relay capture startup on Broadcast entry, and shows an immediate lightweight Broadcast entry bridge while the live camera mounts. + UX16d40a60 Build 775 removes the obsolete view-owned lens fade from in-place CaptureEngine zoom, enlarges and hardens preset tap targets with immediate press feedback, and shows a discreet observed Broadcast source-FPS readout. + UX16d40a61 Build 776 makes RinkLensCameraZoomStore the exclusive applied-zoom writer, removes the permanently locked feature-flag category, gives every remaining temporary rollout a hard Build 785 expiry and replaces self-scored delivery claims with factual evidence. + UX16d40a62 Build 777 makes each committed Broadcast preset a single zoom-store/controller/CaptureEngine transaction, replaces only the Broadcast input branch for Ultra Wide 30fps versus Wide 60fps, retains the OCR branch and MultiCam session, removes the obsolete stable-single-rear plan and fills the entire preset column with interactive rows. + UX16d40a63 Build 778 corrects the Xcode type and actor-isolation boundaries exposed by the first Build 777 device build without changing the requested camera, cadence, tap or recording transaction policy. + UX16d40a64 Build 779 prefers the proven physical Wide MultiCam input, preserves latest same-lens presets, restores last verified logical zoom on failure, removes operator source-switch messaging and makes recording preflight independent of Image Relay. + UX16d40a65 Build 780 consolidates zoom ownership, deletes the legacy HockeyCameraService zoom executor, restores governed recording rollback flags and replaces self-scored/Guard terminology in the changed architecture paths. + UX16d40a66 Build 781 neutralises the stale renamed capture-mode source so folder-synchronised Xcode targets cannot compile duplicate ViewModel declarations; runtime behaviour is unchanged. + UX16d40a67 Build 782 restores the OCR-only applied-device zoom API removed during Broadcast zoom consolidation without restoring the retired Broadcast zoom executor. + UX16d40a68 Build 783 restores only the stateless virtual rear-camera classifier still required by discovery and capture-contract validation after the legacy Broadcast zoom executor was deleted. + UX16d40a69 Build 784 routes capture-generation zoom reconciliation exclusively through CaptureLifecycleController, prevents HockeyCameraService from applying Broadcast physical zoom during camera configuration and aligns logical zoom acknowledgement logging with RinkLensCameraControlStore. + UX16d40a70 Build 785 retains the operator-requested Broadcast zoom when a physical transaction is unavailable, so preset buttons and the slider remain aligned while applied hardware zoom stays separate. + UX16d40a71 Build 785 R1 suppresses stale slider commits after preset taps, makes cross-lens requests latest-request-wins, acknowledges the first verified source frame before recording restoration finishes and removes operator camera-switch progress text. + UX16d40a72 Build 785 R2 deletes the exact-60fps recording rollback and stacked measured-cadence gate, makes recording start single-flight and asynchronous, derives writer cadence from the verified active camera source, releases rejected leases and prevents writer/state divergence. + UX16d40a73 Build 785 R3 makes the active virtual rear camera and capture profile own seamless zoom: 0.5x no longer changes cadence, pauses recording or restores constituent-specific exposure state; AVFoundation zoom ramps use powers-of-two rate semantics. + UX16d40a74 Build 785 R4 deletes the duplicate pending-screen and first-entry bridge presentation paths, mounts Broadcast controls before external OCR capture reconciliation and adds structured route/capture request-completion evidence. + UX16d40a75 Build 785 R5 rejects invalid external OCR exposure samples and saved requests, keeps the USB device in supported automatic exposure instead of locking ISO 0 or NaN shutter state, and records each hardware decision through HockeyCameraService. + UX16d40a76 Build 785 R6 removes the duplicate saved-profile active-camera projection, derives logical camera labels from HockeyCameraService.selectedCameraID, separates requested OCR controls from physical acknowledgements and stops rejected locks being reported as applied. + UX16d40a77 Build 785 R7 prepares the exact recording encoder and compositor contract away from MainActor, releases recording state before background clip/media persistence, serialises deferred clip exports at background priority and moves recording log/Photos registry writes off the route-presentation actor. + UX16d40a78 Build 785 R8 makes Image Relay semantic evidence diagnostic-only, projects viewer semantics from accepted MatchState, removes automatic timeout popups and removes generic Broadcast framing/status progress overlays. + UX16d40a79 Build 785 R9 gives recording priority over an active clip remux, removes manual-clip network optimisation, stops raw penalty evidence invalidating viewer render caches and reduces unaccepted penalty-timer work to a diagnostic cadence. + UX16d40a80 Build 785 R10 preserves the active capture graph for a retained route-paused recording session and rebases writer cadence-health evidence at every intentional pause/resume boundary so stale pause-time zero-FPS evidence cannot immediately fail the resumed file. + UX16d40a81 Build 785 R11 makes CaptureEngine drop telemetry capacity-one: one delayed aggregate flush owns each pending window, removes the competing immediate-threshold publication and no longer mirrors each drop aggregate through MainActor diagnostic breadcrumbs; authoritative counters and the latest immutable CaptureEngine snapshot remain available. + UX16d40a82 Build 785 R12 makes RecordingEngine direct background-writer admission the default temporary rollout, bypassing the duplicate throwaway encoder/compositor warm-up that took 7908ms in R11 while retaining the R11 path behind one removal-bound flag for physical comparison. + UX16d40a83 Build 785 R12a is a compile/install correction only: it removes the stale suffixed 899_BroadcastManualClipPostRollPolicy 2.swift project source so the single canonical 899_BroadcastManualClipPostRollPolicy.swift implementation is compiled; R12 runtime recording behaviour is unchanged. + UX16d40a84 Build 785 R13 makes RinkLensCameraControlStore the sole requested Broadcast image-quality policy owner, derives initial and reconciled Broadcast cadence from that owner instead of a hard-coded 60fps fallback, and coordinates verified same-file RecordingEngine cadence handoff so Motion/Image Quality changes can apply physically during recording without a second policy state or new feature flag. + UX16d40a85 Build 785 R14 fixes RL-016 with deterministic camera-quality modes: Motion and Balanced remain fixed at 60fps, Image Quality remains fixed at 30fps, lighting never changes cadence, duplicate already-applied mode requests are coalesced, and same-resolution MultiCam format switching is used when the current active format cannot supply the operator-requested cadence. + UX16d40a86 Build 785 R15 removes same-cadence camera-quality writer handoffs, primes the hardware encoder without a second recording writer, deletes the failed R11/R12 writer-preparation comparison path, and keeps CaptureEngine warm across Command Centre to remove route-driven Broadcast rebuild delay. + UX16d40a87 Build 785 R16 consolidates the performance architecture: camera capability profiles are built off-main and no longer discovered from screen appearance; Command Centre retains only the preview-critical Broadcast branch; Image Relay heavy frame ownership is admitted through one AppContainer-owned capacity-one non-main pipeline at the Clock service cadence; auxiliary relay lanes are serialised to prevent CPU bursts; BroadcastView observes only its narrow runtime bridge instead of every camera, stream, sponsor and recording store; the ineffective R15 encoder primer is deleted and the one real RecordingWriter emits startup-stage timings; recording Photos persistence completes before deferred clip remux work is released. + UX16d40a88 Build 785 R16a corrects the two Xcode concurrency boundaries exposed by the first R16 simulator compile without changing R16 runtime policy. + UX16d40a89 Build 785 R16b fixes the remaining R16a Xcode compile blocker and compile warnings without changing R16 runtime policy: camera capability profile calculation is pure and returns its off-main result to the existing publication boundary; stale drop-timing state is removed; media dedupe helpers are explicitly nonisolated; deprecated clip-export status logging is removed. + UX16d40a90 Build 785 R17 addresses the R16b measured latency roots: it retains one stable match-session dual-camera graph while route presentation toggles OCR frame delivery, stops ordinary first-frame readiness delay from tearing down a running graph, admits penalty timers only from player-positive evidence, defers clip remux for the complete writer contract including Paused, and pre-arms the same final RecordingWriter so AVAssetWriter.startWriting completes before REC. + UX16d40a91 Build 785 R18 corrects the remaining measured architecture roots: the recording lease protects the output contract rather than one physical rear camera so 0.5x can replace only the Broadcast branch and rebind the same open writer/file; CaptureEngine reports a running graph independently from route frame delivery; unaccepted penalty timer discovery is limited to one repeated player-positive candidate; and the reused Core Image compositor is explicitly prepared on the real writer pixel-buffer pool before REC. + UX16d40a92 Build 785 R19 replaces event-driven zoom churn with one convergent desired-state control plane, negotiates the persistent rear-camera strategy from the actual MultiCam supported set, keeps smooth ramps on one persistent device, removes the 97-toggle Diagnostics runtime surface, migrates current-OS writer preparation to AVAssetWriter.start(), stages writer preparation only after the Broadcast surface is interactive, and serialises heavyweight post-capture media work through MediaRepository. + UX16d40a93 Build 785 R20 simplifies the real-time pipeline: same-device 1x/3x/5x presets go directly to the CaptureEngine AVFoundation ramp while only a genuine 0.5x optical device handoff enters the lens transaction; the zoom store is the sole pending-lens transaction owner; scoreboard processing uses one shared Core Image resource/processor on one serial user-initiated lane with shared multi-field rectification; Broadcast admits Image Relay only after a fresh preview frame; and the one final RecordingWriter is pre-armed only after a stable capture interval. + UX16d40a95 Build 785 R22 freezes one physical Broadcast source for the complete recording file, removes recording-time rear-input replacement and writer source-rebind choreography, coalesces buttons and slider through one CaptureLifecycleController zoom actuator, configures a replacement camera only after its AVCaptureDeviceInput is attached, persists every detected main-thread stall to the structured JSONL, and keeps Image Relay direct-field processing diagnostics explicit. + UX16d40a97 Build 785 Recovery A restores the physically proven Build 770 lens/cadence contract, routes ordinary zoom through CaptureEngine serial execution and removes the R22 MainActor zoom actuator without carrying R23 buffer, relay or writer-prearm experiments. + UX16d40a98 Build 785 Recovery A1 is a compile correction only: it decomposes the Broadcast zoom preset label for Xcode type checking, makes the shared OCR CIContext immutable for Swift 6 isolation, adopts iOS 26 scene geometry APIs and removes the unused zoom preflight binding without changing the Recovery A camera contract. + UX16d40a99 Build 785 Recovery B restores same-file recording 0.5x optical handoff with verified writer cadence rebind, moves FrameHub capture ingress to bounded app-owned CVPixelBuffers, and replays authoritative camera-quality policy after writer closure without changing the physically healthier 1x-5x serial zoom path. + UX16d40b00 Build 785 Recovery C replaces optical-handoff frame polling with the existing event-driven FrameHub waiter, converges the latest requested zoom after immutable handoff completion, and reduces owned-frame ingress copy overhead with measured bulk-copy timing without changing writer ownership, route policy or the working 1x-5x serial zoom path. + UX16d40b01 Build 785 Recovery D makes Image Relay capture requests edge-triggered, balances capture callback QoS, measures complete AVFoundation delegate residence, keeps recording media time continuous through optical handoff and evaluates mixed 30/60 recording health against the weighted verified cadence budget. + UX16d40b02 Build 785 Recovery E isolates external OCR availability changes to the OCR branch of the running MultiCam session, preserving the Broadcast branch/generation and deleting the obsolete full-session reconnect retry waves. + UX16d40b03 Build 785 Recovery F separates image darkness from capture availability: fresh current-generation Broadcast frames are always encoded regardless of luma, source loss is determined only by frame freshness/callback continuity, the expired persistent-black-frame rejection rollout is deleted, and diagnostics report very-dark content without terminating the file. + UX16d40b04 Build 785 Recovery G keeps OCR recovery branch-scoped when Broadcast already satisfies the desired contract and separates requested image-quality cadence from physically effective 0.5x Ultra Wide cadence. + UX16d40b05 Build 785 Recovery H removes stale allow-during-recording capture permission, revalidates recording ownership after lifecycle suspension, and prevents ordinary route/Image Relay/preview/health requests from executing full graph mutations under an open writer while retaining OCR-branch-only convergence that preserves Broadcast. + UX16d40b06 Build 785 Recovery I makes optical rollback physical and truthful: failed post-migration fresh-frame verification compensates back to the exact previous rear device/format/cadence before restoring the old applied zoom, while a failed compensation terminates the zoom transaction against CaptureEngine hardware truth instead of publishing a contradictory logical state. + UX16d40b07 Build 785 Recovery J prewarms the existing bounded Broadcast FrameHub CVPixelBufferPool before real-time delivery, page-touches all six already-permitted owned surfaces, and separates pool-acquisition from pixel-copy timing so rare callback stalls can be attributed without enlarging the pool or retaining AVFoundation buffers. + UX16d40b08 Build 785 Recovery J1 is an Xcode compile correction: FrameHub rebuild diagnostics read the existing locked currentMetrics().rebuildCount API instead of a nonexistent currentRebuildCount helper, and the obsolete iPad 76x76@1x icon declaration is removed for the iOS 26.5 deployment target; Recovery J runtime buffer policy is unchanged. + UX16d40b09 Build 785 Recovery J2 is an install-state compile correction: the J1 source fix is reissued with fail-loud project-folder installation verification so stale Recovery J FrameHub source and the obsolete iPad 76x76@1x icon cannot survive an incremental install; runtime buffer policy is unchanged. + UX16d40b10 Build 785 Recovery K decomposes the remaining RL-034 FrameHub ingress stall into source-lock, destination-lock, memory-copy, unlock, attachment-propagation and residual timings while preserving the six-buffer prewarm and all camera/recording behaviour; no attachment policy is changed until physical evidence identifies the blocking stage. + UX16d40b11 Build 785 Recovery L removes the physically-proven 1.448-second bulk-memory transfer from the AVFoundation Broadcast delegate by introducing one capacity-one source-buffer transfer owner with no queued camera buffers, revalidates generation/device truth across the transfer, and adds app-owned buffer lease attribution without increasing the six-buffer ceiling. + UX16d40b12 Build 785 Recovery L1 corrects the FrameHub owned-lease initializer visibility boundary exposed by the full Xcode target compile and makes the installer replace the complete AppIcon set to remove stale 76x76@1x metadata; Recovery L runtime policy is unchanged. + UX16d40b13 Build 785 Recovery M makes final-writer preparation CaptureEngine-stability driven instead of Command Centre-residency driven: the initial route seeds one event-driven FrameHub fresh-frame wait, the same Broadcast generation/device must remain stable for 1.25 seconds, and the existing sole final RecordingWriter is then pre-armed even if route presentation has changed; REC still owns startSession and the recording timeline. + UX16d40b14 Build 785 Recovery N makes final-writer prearm single-flight under RecordingEngine ownership, releases FrameHub frame leases before the 1.25-second stability suspension and before the expensive AVAssetWriter arm, warms the compositor only after the writer is armed, and derives recording cadence exclusively from CaptureEngine effective format rather than transient observed callback FPS. + UX16d40b15 Build 785 Recovery O restores writer-safe OCR reconnect ownership: external OCR disconnect may remove only the OCR branch while the writer contract is open, reconnect updates availability/desire only until writer close, the expired Build 745 recording-safe OCR reconnect rollout is deleted, and the existing writer-close lifecycle replay performs the one authoritative dual-camera reconciliation after recording. + UX16d40b16 Build 785 Recovery P closes the remaining operator-recovery ownership escape by deferring manual recovery before failure-latch reset whenever the final writer contract is open and requiring the generic recovery stop phase to resolve physically to stopped before any restart or OCR branch convergence can execute. + UX16d40b17 Build 785 Recovery Q removes the remaining route-driven writer-open OCR convergence exception: after satisfied/no-op projection, every ordinary lifecycle request is deferred before admission while the final writer contract is open, and the same hard boundary is revalidated after lifecycle suspension before any OCR branch convergence can execute; explicit teardown/error overrides remain typed, the dedicated optical same-file transaction is unchanged, and writer-close replay remains the sole ordinary OCR restoration trigger. + UX16d40b18 Build 785 Recovery R adds RL-053 wake/suspension forensics only: scene-phase checkpoints capture session running/interrupted state, device discoverability, callback freshness and active FrameHub lease readers; runtime errors include the same context; wake reassertions emit before/after evidence; and the longest released FrameHub lease retains its reader attribution. Runtime capture policy is unchanged. + UX16d40b19 Build 785 Recovery S fixes RL-054 by making the shared scorebug template reserve one invariant maximum utility-strip geometry on every viewer surface; IMAGE/OCR/sponsor content may appear or disappear inside that reserved strip but can no longer alter scorebug height or move the main scoreboard body. Recovery R wake forensics and all camera/recording policy are unchanged. + UX16d40b20 Build 785 Recovery T fixes the RL-053 scene lifecycle race by sequencing background stop and foreground wake through one CaptureLifecycleController-owned transaction queue, evaluating wake intent only after stop completion, removing the duplicate 350ms wake retry and publishing wake completion from verified CaptureEngine truth. + UX16d40b21 Build 785 Recovery U fences Command Centre delayed status work to Command Centre route ownership and adds one deduplicated route-to-preview performance probe with Instruments Points of Interest and structured stage timings for RL-013/RL-032. Camera, recording, zoom, FrameHub and Recovery T scene policy are unchanged. + UX16d40b22 Build 785 Recovery V fixes RL-055 by carrying one immutable Image Relay frame identity through auxiliary work, discarding obsolete queued auxiliary batches before expensive processing, and revalidating capture generation plus monotonic source age immediately before penalty-player and penalty-timer state commits. Stale work can no longer start, confirm, clear, rebind or publish current penalty evidence; no retry, delay, mirror state or feature flag is added. + UX16d40b23 Build 785 Recovery W fixes RL-056 by replacing FIFO auxiliary Image Relay history with one active plus one replaceable pending latest-frame request, merging due field intents onto the newest immutable frame, rendering crops lane-by-lane, rebinding unprocessed lanes when newer evidence arrives and fencing stale score/period publication. Recovery V freshness protection and all camera, recording, zoom, FrameHub and scene-lifecycle policy remain unchanged. + UX16d40b24 Build 785 Recovery X fixes RL-057 by making required first-frame readiness independent from route presentation, requiring both physical branches for dual-camera success, revalidating device discoverability and graph structure at readiness commit, fencing OCR delivery against missing/degraded hardware, and keeping a structurally reattached OCR branch degraded until a fresh current-generation OCR frame verifies restoration. Recovery W/V Image Relay policy and recording, zoom, FrameHub and scene-lifecycle ownership remain unchanged. + UX16d40b25 Build 785 Recovery Y fixes RL-058 by making RecordingEngine own one recording-camera handoff boundary and one pending pause intent: route, Command Centre and operator pause requests defer writer mutation until the active optical handoff completes or aborts, a Paused writer cannot start a second recording-camera handoff, and a deferred Command Centre pause replays current route intent only after the handoff boundary releases the capture lease. + UX16d40b26 Build 785 Recovery Z fixes RL-059 by separating saved logical OCR source ownership from physical camera availability: temporary USB absence cannot replace External Camera with Built-in Back, unresolved logical staging retains the selected source with no applied physical device, automatic preparation cannot override an existing selection, and startup/default reconciliation re-projects the saved calibration source instead of choosing another available camera. Recovery Y recording transaction ownership and Recovery X/W/V capture and Image Relay protections remain unchanged. + UX16d40b27 Build 785 Recovery Z1 is a compile/install correction only: Calibration camera warnings remain mutated by HockeyScoreboardViewModel through an explicit owner API, and AppIcon installation verifies no obsolete iPad 76x76@1x metadata survives. Recovery Z RL-059 runtime camera-source ownership is unchanged. + UX16d40b28 Build 785 Recovery AA keeps cross-domain slider-live values presentation-local until commit, prevents immutable optical convergence from chasing transient slider values, seeds the saved rink camera profile before generic startup defaults, and preserves valid unresolved logical camera selection across physical staging rollback. + UX16d40b29 Build 785 Recovery AB fixes RL-060 by making degraded Broadcast-only fallback preserve a rear physical device that remains compatible with the still-desired OCR camera and by scoping failed-contract cooldown to full graph rebuilds rather than safe OCR-branch-only convergence; generic Broadcast-only startup retains the existing virtual-rear preference, and no retry wave, delay or feature flag is added. + UX16d40b30 Build 785 Recovery AC fixes RL-061 by separating FrameHub pixel ownership from freshness acknowledgement: lifecycle, writer-prearm, recording preflight and route-control paths receive immutable frame evidence only, while CVPixelBuffer leases remain confined to consumers that actually process pixels. Event-driven evidence waiters cannot retain the six-surface pool across suspension; no pool-size increase, retry, delay, debounce or feature flag is added. + UX16d40b31 Build 785 Recovery AD fixes RL-064 by removing FrameHub pixel-buffer leases from the externally managed HockeyCameraService compatibility facade: CaptureEngine frame notifications are projected through immutable FrameHub evidence only, the facade no longer opens the current pixel lease merely to update frame-health/pipeline metadata or detect a newer sequence, and legacy self-owned capture keeps its existing pixel path. CaptureEngine, RecordingEngine/Writer, zoom, Image Relay scheduling and the six-buffer ceiling are unchanged; no retry, delay, debounce or feature flag is added. + UX16d40b32 Build 785 Recovery AE fixes RL-065 by making saved rink/camera profile restoration a side-effect-free hydration transaction: startup rotation/profile values write directly to their authoritative stores without dirtying calibration, startup template application does not issue camera hardware requests, and the saved OCR format plus manual locks are applied once only after the physical OCR source is staged through the CaptureEngine startup transaction. No retry, delay, debounce or feature flag is added. + UX16d40b33 Build 785 Recovery AF adds one execution-admission authority for expensive non-real-time work: operator route transitions and the one final RecordingWriter prearm pre-empt Image Relay auxiliary processing and offline post-capture media work, active clip remux yields back to pinned deferred state, and work resumes explicitly only after route/prearm boundaries release. CaptureEngine callbacks, FrameHub ingress, the RecordingWriter hot path and the Image Relay Clock fast lane remain outside this coordinator. No retry, delay, debounce or feature flag is added. + UX16d40b34 Build 785 Recovery AG isolates the remaining cross-subsystem boundaries exposed by Recovery AF: FrameHub copies pixels plus an explicit bounded CoreVideo colour-metadata allow-list instead of propagating every opaque attachment; BroadcastView observes only its BroadcastRuntimeViewModel while layout, sponsor and stream stores remain mutation owners behind one immutable presentation projection; and Image Relay score/period/penalty-player auxiliary work revalidates execution admission and immutable frame identity between expensive stages so obsolete work exits before final commit. CaptureEngine, CaptureLifecycleController, RecordingWriter hot-path, optical zoom and the six-buffer ceiling are unchanged; no retry, delay, debounce or feature flag is added. + UX16d40b35 Build 785 Recovery AH separates the physical OCR capture plane from consumer processing and SwiftUI presentation: route/Image Relay state no longer toggles the OCR AVCaptureConnection or clears FrameHub; external UVC saved controls are capability-filtered and invalid exposure placeholders are device-owned; physical OCR pixel-health evidence remains available across route changes. No retry, delay, debounce or feature flag is added. + UX16d40b36 Build 785 Recovery AI moves capacity-one scoreboard frame admission ahead of MainActor, keeps FrameHub pixel leases out of UI scheduling, makes Image Relay/OCR orchestration explicitly non-main, removes continuous decorative Calibration pixel work, samples OCR luminance per capture generation and makes Recover Preview preview-endpoint-first when the physical branch is fresh. No pool increase, retry wave, delay, debounce or feature flag is added. + UX16d40b37 Build 785 Recovery AJ removes production Image Relay's per-frame synchronous MainActor preparation, derives one immutable non-main execution projection only when authoritative state changes, and replaces the ViewModel's unconditional 15-owner SwiftUI rebroadcast with AppCoordinator-route-scoped presentation invalidation. Legacy OCR compatibility remains bounded and asynchronous; camera, writer, zoom and match semantics are unchanged. No retry, delay, debounce, pool increase or feature flag is added. + UX16d40b38 Build 785 Recovery AK makes scoreboard-frame execution explicitly inactive/Image-Relay/OCR-compatible so suspended frames cannot fall into MainActor preparation, and consolidates live Image Relay Core Image work behind one serial scoreboard processor with one lazy perspective-corrected board render shared by Clock, Score, Period and penalty field crops from each executed source frame. Recovery AJ route-scoped invalidation and Recovery AI FrameHub ownership remain unchanged; AVAssetWriter prearm is deliberately unchanged for independent physical measurement. No retry, delay, debounce, pool increase or feature flag is added. + UX16d40b39 Build 785 Recovery AL isolates the final-writer codec-acquisition boundary from scoreboard pixel processing: the AK Image Relay processor yields completely while AVAssetWriter prearm is critical, queued Clock work is discarded rather than becoming stale, prearm executes at utility QoS on the same authoritative RecordingWriter serial queue, and the expired fixed-deadline/independent-Clock rollback branches are deleted so the fixed-deadline scheduler is unconditional. CaptureEngine and FrameHub remain live for freshness; no retry, delay, debounce, pool increase or new feature flag is added. + UX16d40b40 Build 785 Recovery AL1 is an Xcode compile/install correction only: it force-delivers the AK shared-board crop API and caller together, makes the background scoreboard processing types explicit under the target-wide MainActor default, fixes the writer-prearm concurrent capture, and hardens installation verification without changing Recovery AL runtime policy. + UX16d40b41 Build 785 Recovery AL2 completes the Xcode target-boundary correction: it force-replays the typed execution-plan ViewModel, fixes the Clock stable-duration declaration order, makes pure OCR/model/replay contracts explicitly nonisolated, confines recording-overlay cache invalidation to an explicit MainActor presentation hop, and ships the corrected AppIcon metadata in the incremental delta. Recovery AL runtime policy is unchanged. + UX16d40b42 Build 785 Recovery AM fixes RL-082 and RL-032: one serial scoreboard rectification authority now feeds independent Clock and auxiliary service queues so multi-second semantic work cannot head-of-line block the 0.30-second viewer Clock, and one persistent RootRouter-lifetime Broadcast host stays mounted while AppCoordinator owns visibility rather than destroying/recreating the live surface on every route change. CaptureEngine, FrameHub, writer hot path and optical zoom are unchanged; no retry, delay, debounce, pool increase or feature flag is added. + UX16d40b43 Build 785 Recovery AN fixes RL-032B and RL-083: Broadcast keeps session-lifetime state/runtime identity but mounts its expensive preview/compositor/operator subtree only while visible, and the existing execution coordinator admits deferred clip/Photos/media work only by reading authoritative CaptureEngine pressure, current-generation drop and callback-freshness truth at the moment of admission, with a final recheck before opaque AVAssetExportSession work starts. Recovery AM Clock topology and all camera, writer, FrameHub and zoom ownership are unchanged; no retry, delay, debounce, pool increase or feature flag is added. + UX16d40b44 Build 785 Recovery AO fixes RL-076/RL-084/RL-085 by making Calibration/OCR Setup render the authoritative latest OCR FrameHub pixels instead of an independent OCR AVCaptureVideoPreviewLayer, adding an event-driven value-only FrameHub preview notifier and displayed-sequence acknowledgement, and deleting the delayed 250/750ms legacy OCR preview reconnect choreography. CaptureEngine remains the sole camera owner; OCR/Image Relay recognition, Broadcast preview, recording, zoom and FrameHub six-surface capacity are unchanged; no retry, delay, debounce, pool increase or feature flag is added. + UX16d40b45 Build 785 Recovery AP makes Command Centre configuration-only before first operational capture, prepares one persistent match-session VideoToolbox H.264/H.265 compressor only after a verified Broadcast/Recording route is live, reuses that compressor across recording files, writes compressed samples through an AVAssetWriter passthrough muxer, deletes the obsolete speculative AVAssetWriter prearm path and adds rendered OCR preview luminance/alpha acknowledgement; no retry, delay, debounce, pool increase or new feature flag is added. + UX16d40b47 Build 785 Recovery AP2 removes the final stale ViewModel prearm cancellation call and hardens exact-target asset installation without changing Recovery AP runtime architecture. + UX16d40b48 Build 785 Recovery AQ makes full-match recording capture-event-driven from CaptureEngine through one bounded active+latest-pending ingress mailbox, deletes the RecordingWriter timer/FrameHub polling transport, and leaves FrameHub as OCR/preview/control evidence rather than recording camera truth. + UX16d40b49 Build 785 Recovery AR fixes RL-095 by making CaptureEngine synchronously copy every camera callback once into the existing prewarmed application-owned FrameHub pool before fan-out, feeding AQ recording from that exact owned lease, and deleting Recovery L raw-camera asynchronous transfer ownership. + UX16d40b50 Build 785 Recovery AS fixes RL-096/RL-097 by giving RecordingWriter a separate bounded three-surface ingress pool, synchronously copying each short-lived FrameHub lease into recording-owned memory before mailbox admission, exporting exact ingress accounting and preventing recording/compositor/encoder lifetime from exhausting the producer FrameHub pool. No FrameHub pool increase, retry, delay, debounce or new feature flag is added. + UX16d40b52 Build 785 Recovery AT1 is a compile/install correction only: it preserves Recovery AT RL-099, fixes RecordingWriter optional configuration and explicit closure capture boundaries, makes recording-frame/value analysis contracts explicitly nonisolated, and force-replays the AS FrameHub pool and recording frame provider so stale incremental sources cannot remove the recording-owned pool initializer or monotonic capture timestamp. Runtime media policy is unchanged. + UX16d40b53 Build 785 Recovery AU fixes RL-100 by making live CaptureEngine session ownership a hard media-resource lease: opaque clip export and Photos persistence cannot start while capture is active/running, route presentation no longer resumes deferred media, and only verified capture stop can re-admit the pinned MediaRepository/ClipEngine queue. No retry, delay, debounce, pool increase or feature flag is added. + UX16d40b54 Build 785 Recovery AV is the one-week convergence build: RL-101 atomic saved-scorebug bootstrap and stale preview-retention deletion; RL-102 persistent Broadcast preview mounting across route visibility; RL-103 Broadcast FrameHub value-only evidence with RecordingWriter as the sole required pixel-copy owner; RL-104 terminal optical zoom requested/applied reconciliation to verified hardware truth; and RL-105 direct Clock crop isolation from full-board auxiliary Image Relay rectification. Recovery AU media ownership and the healthy final RecordingWriter/encoder contract are preserved; no retry, delay, debounce, pool increase or new feature flag is added. + UX16d40b55 Build 785 Recovery AW convergence-2 fixes the Recovery AV physical regressions: RL-106 makes saved-profile Load permanently visible and removes the fixed 400pt Profiles popover; RL-107 closes RecordingWriter pixel-copy admission before Paused/stopping/optical-held frames; RL-108 suspends scoreboard processing on every non-camera route while independently retaining required capture; RL-109 bounds OCR preview MainActor display to active+latest pending; and RL-110 reasserts the already-resolved Broadcast cadence after MultiCam start. Recovery AV RL-101–RL-105 remain cumulative; no retry, delay, debounce, pool increase or new feature flag is added. + UX16d40b56 Build 785 Recovery AX convergence-3 fixes the remaining AW measured contention: repeated saved-profile work is made idempotent with stable decoded-logo identity and one batched scorebug persistence/log transaction; the 0.30-second direct Clock is no longer denied by every short recording-frame critical section and always promotes the newest pending physical frame before processing; recording UI progress publication is reduced to 1 Hz while internal frame/cadence accounting stays unchanged. Recovery AW/AV/AU camera, writer, zoom, FrameHub and media-resource ownership remain cumulative; no retry, delay, debounce, pool increase or new feature flag is added."
    static let currentArchitectureNote = "UX16d40b144 Build 785 Recovery EB fixes RL-267, RL-268 and RL-269 from the 19:04 physical run: the full Broadcast zoom bar is permanently visible again; the stream panel changes its Go Live control to a green elapsed LIVE acknowledgement only after physical publishing; and accepted Image Relay penalty pairs commit one atomic four-slot player snapshot through MatchState so popup and scorebug presentation share one semantic acceptance boundary. CaptureEngine optical handoff, RecordingWriter and HLS transport mechanics are unchanged."
    static let buildNumber = 785
    static let buildDateTime = "2026-09-01 20:05 BST"
    static let releaseNote =
        "UX16d40b144 Build 785 Recovery EB addresses RL-267, RL-268 and RL-269 from the supplied 19:04-19:07 run. It removes Build 143's compact zoom replacement and restores the full continuous Broadcast zoom bar without changing camera ownership. The Broadcast Stream Go Live control now becomes a green checkmarked LIVE indicator with elapsed publishing time only after StreamControlStore has physically acknowledged programme publication; Requested and Starting remain distinct. Finally, the Image Relay physical player+timer pair reconciler still owns OCR acceptance, but now submits its complete four-slot player snapshot through the authoritative MatchState reducer in the same transaction. This removes the split truth that created a penalty popup while the viewer gate still reported zero accepted penalties. Image Relay retains timer-pixel ownership. RecordingWriter, CaptureEngine optical handoff and HLS transport mechanics are unchanged. Full iPad acceptance remains required."

    static var version: String {
        "v\(semanticVersion)-\(releaseCode)"
    }

    static let canonicalSourceFile = "038_RinkLensBuildIdentity.swift"

    static var buildIdentityWitness: String {
        "\(version)|build=\(buildNumber)|source=\(canonicalSourceFile)"
    }

    static var buildDisplayLine: String {
        "Build \(buildNumber) • \(buildDateTime)"
    }

    static var diagnosticsExportVersionLine: String {
        "Export file version: \(version) • Build \(buildNumber) • source \(canonicalSourceFile) • witness \(buildIdentityWitness) • diagnostics export completion logs - \(diagnosticArchitecture) + \(currentArchitectureNote)"
    }

    static func traceContext(_ message: String) -> String {
        "\(releaseCode) \(message)"
    }
}
